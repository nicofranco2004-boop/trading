# Railway deploy config. La configuración de build (Python, deps, etc.) vive
# en nixpacks.toml — Railway lo lee automáticamente.

[build]
builder = "nixpacks"

[deploy]
restartPolicyType = "on_failure"
