# Rendi backend — environment variables
# ═══════════════════════════════════════════════════════════════════════════
# Copiá este archivo a `backend/.env` y completá los valores.
# `.env` está ignorado por git (ver .gitignore root) — nunca se commitea.
#
# En producción (Railway), estas variables se setean en el dashboard de la
# plataforma y este archivo no se usa.

# ─── AI Coach ────────────────────────────────────────────────────────────────
# Required para que el "Coach IA" de /insights funcione.
# Obtenela en: https://console.anthropic.com/settings/keys
# Modelo usado: Claude Haiku 4.5 (más barato, suficiente para chat con contexto).
ANTHROPIC_API_KEY=sk-ant-api03-REPLACE_ME

# ─── Auth ────────────────────────────────────────────────────────────────────
# Required para que los tokens JWT no se reseteen al reiniciar uvicorn.
# Generar con: python3 -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=

# ─── Admin (opcional) ────────────────────────────────────────────────────────
# Hash SHA-256 del email admin. Si está vacío, no hay admin.
# Generar con: python3 -c "import hashlib; print(hashlib.sha256('tu@email.com'.encode()).hexdigest())"
ADMIN_EMAIL_HASH=

# ─── CORS (opcional) ─────────────────────────────────────────────────────────
# Default: localhost:5173,localhost:5174 (Vite dev). En prod agregar dominios.
# ALLOWED_ORIGINS=https://rendi.finance,https://www.rendi.finance

# ─── Database (opcional) ─────────────────────────────────────────────────────
# Default: ./backend/trading.db. En Railway: /data/trading.db (volume).
# DB_PATH=/data/trading.db

# ─── Push notifications (Web Push API) ──────────────────────────────────────
# Required para que las notificaciones (earnings, drawdown, behavioral) lleguen.
# Generar el par con:
#   python3 -c "
#   from py_vapid import Vapid
#   import base64
#   v = Vapid(); v.generate_keys()
#   pn = v.public_key.public_numbers()
#   pub = base64.urlsafe_b64encode(b'\\x04' + pn.x.to_bytes(32,'big') + pn.y.to_bytes(32,'big')).rstrip(b'=').decode()
#   priv = base64.urlsafe_b64encode(v.private_key.private_numbers().private_value.to_bytes(32,'big')).rstrip(b'=').decode()
#   print('PUB=', pub); print('PRIV=', priv)
#   "
# Subject: mailto:tu@email.com — required por el spec de WebPush.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:hola@rendi.finance

# ─── Rebill (Billing — payment processor activo) ────────────────────────────
# Required para todas las suscripciones (Plus, Pro). Dashboard:
# https://app.rebill.com (sandbox) | https://app.rebill.com (prod después del approve)
#
# La API key define el ambiente automáticamente:
#   - sk_test_... → sandbox (no se cobra dinero real)
#   - sk_live_... → producción
# Settings → API Keys del dashboard de Rebill.
REBILL_API_KEY=
# Secret del webhook que configurás en Settings → Webhooks de Rebill.
# Sin esto, los webhooks se aceptan sin validar signature (warning en logs).
REBILL_WEBHOOK_SECRET=
# Plan IDs (4 combinaciones). Los creás en Plans del dashboard Rebill y
# copiás el ID de cada uno. En sandbox arrancan con `test_pln_`, en prod
# con `pln_`. Mismatch entre ambiente de la key y prefijo del plan → error
# al startup.
REBILL_PLAN_ID_PLUS_MONTHLY=
REBILL_PLAN_ID_PLUS_ANNUAL=
REBILL_PLAN_ID_PRO_MONTHLY=
REBILL_PLAN_ID_PRO_ANNUAL=

# ─── Mercado Pago (Billing — LEGACY, no se usa) ─────────────────────────────
# DEPRECATED: migramos a Rebill. Las vars quedan acá por si hay que revertir
# en una emergencia, pero el código nuevo (billing/rebill.py) las ignora.
# Eliminar este bloque después de 30 días sin problemas en prod.
MP_ACCESS_TOKEN=
MP_PUBLIC_KEY=
MP_ENV=sandbox
MP_WEBHOOK_SECRET=
# URL base del frontend para los return_urls del checkout
# En dev: http://localhost:5173, en prod: https://rendi.finance
MP_FRONTEND_BASE_URL=http://localhost:5173

# ─── Email transaccional (Resend) ───────────────────────────────────────────
# Required para que los emails de billing se envíen realmente. Sin esto, los
# eventos se loguean a console (modo dev / no-op).
# Crear API key en: https://resend.com/api-keys
# Free tier: 100 emails/día, 3000/mes.
RESEND_API_KEY=
# From addresses — el dominio rendi.finance está verificado en Resend.
#
# Rendi usa 3 alias según el tipo de email:
# - no_reply@: transaccionales sin respuesta esperada (recibos, OTPs, welcomes,
#   cancelaciones, recordatorios de vencimiento)
# - soporte@:  emails donde el user puede contestar (password reset, alerta
#   de nuevo login, pago fallido) → replies caen al inbox vía alias
# - hello@:    reservado para marketing / onboarding manual (default fallback)
#
# Todos son alias del usuario nicolas@rendi.finance en Workspace.
EMAIL_FROM=Rendi <hello@rendi.finance>
EMAIL_FROM_NOREPLY=Rendi <no_reply@rendi.finance>
EMAIL_FROM_SUPPORT=Rendi Soporte <soporte@rendi.finance>
