# Preguntas abiertas candidatas — benchmarks

29 preguntas del mapeo previo que matchean este tema por palabra clave.
**Filtro automático, no curado:** puede traer ruido y puede faltar alguna.
Elegí SOLO las que necesitás que el founder conteste para poder concluir.

- _[API]_ P-002** | 🔴 bloquea | Endpoints de API (main.py 8500–11000): p | ¿Cuántas cuentas ya cobraron un plazo fijo EN PESOS? Es la pregunta que decide si el bug de `Interés PF` (pesos contados como dólares en el P&L realizado y en el win-rate) es teórico o ya está mordiendo. El comentario de backend/realized_pnl.py:66 dice '0 filas' pero está fechado. 
- _[API]_ P-003** | 🔴 bloquea | Endpoints de API (main.py 8500–11000): p | El alta de un plazo fijo debita el cash del broker y el borrado no lo devuelve, porque `source_broker` no se guarda en ninguna parte. ¿Cuánta plata fantasma hay hoy por PFs dados de alta con origen y después borrados o editados? 
- _[API]_ P-004** | 🔴 bloquea | Endpoints de API (main.py 8500–11000): p | Hay filas de posición que NO se pueden borrar desde la UI (las manuales viejas sin `undo_meta_json` reciben un 400 bloqueante, backend/main.py:8639). ¿Cuántas hay y qué se le dice al usuario que quiere sacarlas? 
- _[API]_ P-006** | 🔴 bloquea | Endpoints (main.py 5500–8500): benchmark | El rename grupal cascadea a `operations` con el par de brokers pero sólo renombra los lotes del broker exacto. ¿Es intencional (el sibling se renombra aparte) o es el bug que sospecho? 
- _[API]_ P-039** | 🟡 afecta la interpretación | Endpoints (main.py 5500–8500): benchmark | ¿Alguien depende de que /api/positions devuelva `undo_meta_json` y `user_id`? Si no, conviene proyectar columnas explícitas. 
- _[API]_ P-040** | 🟡 afecta la interpretación | Endpoints (main.py 5500–8500): benchmark | ¿Por qué /api/prices/history usa auto_adjust=False mientras /api/prices y prev-close usan True? El mini-chart y el precio de la tabla salen de series ajustadas distinto. 
- _[API]_ P-044** | · menor | Endpoints (main.py 2680–5500) | POST /api/snapshots acepta el total_value calculado por el browser sin ninguna validación de plausibilidad más allá de ge=0, y esa serie es la base de rendimientos, máximos y comparación contra benchmark. ¿Hay algún clamp aguas abajo o esto es confianza total en el cliente? 
- _[API]_ P-050** | · menor | Endpoints (main.py 5500–8500): benchmark | ¿El calendario macro hardcodeado (MACRO_EVENTS_CALENDAR, backend/main.py:5734-5788) tiene dueño y recordatorio para renovarse en enero 2027, o el tab 'Popular' se va a vaciar solo? 
- _[API]_ P-051** | · menor | Endpoints (main.py 5500–8500): benchmark | ¿'UVA' y 'A3500' en la allowlist de /api/bond-indices son features planeadas o restos de un diseño que nunca se implementó? Hoy devuelven serie vacía con stale:true permanente. 
- _[API]_ P-052** | · menor | Endpoints (main.py 5500–8500): benchmark | ¿Los dos endpoints de escritura del tramo (POST /api/positions, PATCH /api/positions/group) tenían que reintentar ante 'database is locked'? Porque el decorador está puesto pero no funciona — habría que sacar el `except Exception` o sacar el decorador para no dar falsa sensación de cobertura. 
- _[API]_ P-053** | · menor | Endpoints (main.py 5500–8500): benchmark | Con el mercado cerrado, ¿está bien que la variación diaria de todos los .BA muestre 0 (backend/main.py:7917) en lugar del movimiento de la última rueda? Es una decisión de producto tanto como técnica. 
- _[API]_ P-058** | · menor | Endpoints (main.py 13500–16000) | El diagnóstico de objetivos ignora `modo`/`moneda` y descarta el acumulado cuando el motor no anualiza. ¿Se alinea con `/api/goals/cagr` (pasarle los dos params y usar `total_return_pct` cuando `cagr` es None)? 
- _[API]_ P-060** | · menor | Endpoints (main.py 18500–21000) — panel  | ¿Sabías que /api/admin/delete-snapshot y /api/admin/wipe-broker-data solo tocan TU cuenta y no la de un usuario objetivo? Si la intención era soporte sobre terceros, hoy no hay ningún endpoint que lo permita. 
- _[API]_ P-076** | · menor | Endpoints (main.py 16000–18500) | `repair-comisiones` (backend/main.py:17706) hace `objetivo = objetivo[:limite]` ANTES de calcular `comisiones_falsas_total` y `usuarios`, sin reportar cuántas quedaron afuera — justo lo que `check-invariantes` declara inaceptable en backend/main.py:17792. Con más de 5.000 posiciones afectadas, ¿el número que viste al decidir aplicarlo era el real o el truncado? 
- _[Conceptos de negocio]_ P-129** | · menor | CEDEAR: ratio, split, valuación y pata e | ¿Sabías que hay filas con asset_type='cedear' en minúscula (lo dice el test assetClass.test.js:114)? Para esas filas el backend pide el .BA y no lo lee, y el frontend las clasifica como CEDEAR en la torta pero las precia como acción US. ¿Normalizamos la columna en una migración, o normalizamos todas las comparaciones? 
- _[Conceptos de negocio]_ P-145** | · menor | P&L realizado | ¿'P&L realizado' en Reportes y Dashboard tiene que incluir dividendos, cupones e intereses? Hoy los incluye (y el benchmark ^SP500TR se eligió asumiendo que sí, main.py:5340-5342), mientras que el mismo concepto en el chat de la IA no. Elegir uno es una decisión de producto, no una limpieza. 
- _[IA]_ P-157** | 🔴 bloquea | Tenencia / posición / holding | Los plazos fijos entran al patrimonio del Dashboard pero no al snapshot del cron: ¿es intencional que la variación diaria se calcule contra un total que excluye el plazo fijo? 
- _[IA]_ P-159** | 🔴 bloquea | Subsistema de IA | ¿`metrics_pro.card` e `insights.benchmarks` son features pendientes de cablear o restos a borrar? Son ~320 líneas de builder + prompt sin ningún consumidor. 
- _[IA]_ P-169** | · menor | P&L no realizado / ganancia latente | ¿Los intereses devengados de un plazo fijo son «P&L no realizado»? Hoy entran al KPI del Dashboard pero no a `monthly_entries` ni a los snapshots ni al snapshot que ve la IA. 
- _[Modelo de datos]_ P-194** | 🔴 bloquea | Modelo de datos — núcleo de negocio (use | `realized_pnl.py:63-71` dice que `'Interés PF'` guarda moneda nativa como un cupón, no está excluido de `closed_filter_sql` y no se convierte: hoy 0 filas en prod. ¿Se cierra ahora o se espera al primer plazo fijo en pesos que alguien cobre? 
- _[Modelo de datos]_ P-207** | · menor | Modelo de datos — núcleo de negocio (use | `positions` tiene un único índice (`user_id`, main.py:1220) mientras el patrón real de query es `(user_id, broker, asset, is_cash)`. ¿Hay una razón para no tener el compuesto que sí tienen `operations` y `monthly_entries`? 
- _[Modelo de datos]_ P-210** | · menor | Modelo de datos auxiliar: cache, mercado | ¿Hay alguna razón por la que `news`, `financial_events` y `bond_indices_daily` guarden `fetched_at`/`updated_at` si la frescura la decide un dict en memoria? Mover el TTL a la columna arreglaría de una el re-fetch masivo de cada redeploy y el `stale` inconsistente entre workers. 
- _[Motores de retorno]_ P-230** | 🔴 bloquea | TIR / IRR / XIRR / retorno ponderado por | main.py:15305 (_cagr_from_monthly_rows) divide por `ci` pelado sin el ½·flujo y clampa a +500 %. Ese CAGR alimenta el diagnóstico de Objetivos y los packets de IA. ¿Es intencional que ese camino NO sea Modified Dietz? 
- _[Motores de retorno]_ P-237** | · menor | TIR / IRR / XIRR / retorno ponderado por | La card de Objetivos muestra dos tasas requeridas a 15 píxeles de distancia ('Solo con rendimiento' del frontend y 'Necesario · X%/año' del backend), calculadas sobre tres capitales distintos y dos formas de contar meses. ¿Se puede matar una de las dos? 
- _[Motores de retorno]_ P-240** | · menor | Rendimiento / retorno (simple, TWR, anua | Los tres packets de IA (insights, insights.evolution, reports) y el Wrapped usan una fórmula que no es Modified Dietz y sobreestima con aportes. insights_benchmarks ya se migró al motor canónico. ¿Querés que los otros cuatro sigan el mismo camino, o hay una razón por la que el Wrapped tenga que salir de monthly_entries? 
- _[Motores de retorno]_ P-241** | · menor | Rendimiento / retorno (simple, TWR, anua | El CAGR se publica con dos pisos distintos: medio año en Dashboard/Objetivos (motor canónico) y dos meses en el generador metric_cagr del Diagnóstico. ¿Cuál es el piso que querés que valga? Hoy el mismo usuario con 3 meses ve un anualizado en una pantalla y un «Desde que medimos» en la otra. 
- _[Motores de análisis]_ P-247** | 🔴 bloquea | Motores de análisis: reporting, behavior | Cuando el CAGR histórico no se puede anualizar (ventana < medio año), ¿qué debería decir el diagnóstico del objetivo? `_historical_cagr_global` ya devuelve `total_return_pct` con su ventana para poder decirlo bien (backend/main.py:15379-15381) y `goals_diagnostic` no los mira. 
- _[Motores de análisis]_ P-251** | 🔴 bloquea | Frontend — Análisis: Métricas, Comportam | ¿El toggle 'Costo en dólares' (costBasis) debería afectar a Objetivos? Goals.jsx llama computeBrokerValue sin ese parámetro y Insights sí lo pasa. 
- _[Motores de análisis]_ P-253** | 🔴 bloquea | Frontend — Análisis: Métricas, Comportam | El veredicto 'Le ganás / Le perdés' al S&P sale de un motor distinto que el gráfico que está justo arriba. ¿Se unifica al backend (perf.benchmark) o el veredicto flow-matched es deliberadamente otra pregunta? 
