- ¿DATABASE_URL está seteada hoy en Railway? Si la respuesta es sí, hay que revisar YA los tres agujeros del §1.7 (el cron de snapshots escribe a SQLite, el diagnóstico de disco deja de servir, y el backup diario está respaldando la base equivocada).
- ¿Por qué start-rendi.sh:43 tiene una SECRET_KEY commiteada? ¿Esa clave se usó alguna vez en una instancia que sirviera tráfico real? Si sí, hay que rotarla y todos los tokens vivos firmados con ella.
- ¿Dónde está CORRECTNESS_AUDIT_2026-06-25.md? Once puntos del código (incluido el gate de SECRET_KEY y la regla de .gitignore sobre backups con hashes bcrypt) apuntan a un documento que no está en el repo.
- ¿El backup diario a ./backups/ sigue viviendo en el mismo volumen de Railway que la base? MIGRACION_POSTGRES.md:2290-2292 proyecta que el disco vuelve al 97% 'entre el 8 y el 28 de septiembre' — o sea ahora. ¿Están seteadas las BACKUP_S3_* o el único backup es local?
- ¿Con qué se verifica un deploy hoy? No hay CI: 241 archivos de test back y 55 front, y nada los corre antes de que Railway/Vercel construyan.
- ¿start.sh se usa todavía? Está roto desde que seed.py se deprecó (sys.exit(1) a nivel de módulo mata el script por el set -e).
- El scheduler es in-process con un solo worker uvicorn (nixpacks.toml:31, sin --workers). ¿Eso es una decisión (SQLite = un solo escritor) o una herencia? Es el techo de concurrencia de todo el backend.
- El backfill de `positions.currency` (backend/main.py:980-994) corre en CADA arranque, sin guard de columna, para todos los usuarios, y reclasifica por la moneda del BROKER cualquier lote que tenga `currency` NULL. Varios INSERT manuales no setean `currency` (main.py:4102, 9782, 10078, 10170): ¿es deseado que un lote cargado a mano cambie de moneda en el próximo reboot?
- `config.display_currency` lo lee `backend/ai/builders/metrics_pro_card.py:126-130` pero NADIE lo escribe: el frontend guarda la moneda en localStorage (`frontend/src/contexts/CurrencyContext.jsx:24`). ¿El card de métricas de la IA debería leer la moneda por otro lado, o hay que persistirla en `config`?
- `futures_positions.liquidation_price` no tiene lector ni input en el formulario, y `plazos_fijos.renovacion_auto` se guarda pero ninguna lógica lo consulta. ¿Son features a medio hacer o se pueden retirar?
- `realized_pnl.py:63-71` dice que `'Interés PF'` guarda moneda nativa como un cupón, no está excluido de `closed_filter_sql` y no se convierte: hoy 0 filas en prod. ¿Se cierra ahora o se espera al primer plazo fijo en pesos que alguien cobre?
- `delete_broker` (main.py:4339) NO limpia `import_normalized_tx`, `bond_cashflow_skips`, `futures_positions`, `deleted_ops_journal` ni el `broker` embebido en `archived_positions.payload`. `NAME_KEYED_TABLES` (main.py:4127-4134) tampoco incluye las tres últimas para el rename. ¿Se agregan al cascade o hay una razón para dejarlas?
- Cuatro tablas de mi alcance (`archived_positions`, `futures_positions`, `watchlist`, `deleted_ops_journal`) están fuera del allowlist de `_table_cols` (main.py:578-582), el mismo agujero que ya rompió `advisor_op_batch_items` en prod. ¿Se agregan preventivamente, o se cambia el patrón a `PRAGMA table_info` directo como en main.py:904?
- `positions` tiene un único índice (`user_id`, main.py:1220) mientras el patrón real de query es `(user_id, broker, asset, is_cash)`. ¿Hay una razón para no tener el compuesto que sí tienen `operations` y `monthly_entries`?
- `operations.pnl_pct` y `operations.cost_basis_consumed`: la segunda está 100% NULL según tres comentarios del propio código. ¿Se dejan como está, se llenan, o se retiran del schema?
- ¿Está seteada ALERTS_CRON_TOKEN en Railway? Si no lo está, el cron de alertas devuelve 503 y ni las alertas de precio ni las del libro del asesor se están evaluando en producción (backend/main.py:33597-33599).
- asset_price_history + price_backfill_log + ledger_replay están construidos y testeados pero SIN ningún caller en producción (ni endpoint ni cron). ¿Es infraestructura parqueada a propósito para el agente reconstructor, o se perdió el cableado en algún merge?
- SECRET_KEY cifra las credenciales de broker (Fernet sobre sha256(SECRET_KEY)) Y firma los JWT. ¿Está asumido que rotarla por seguridad de sesiones borra en el acto todas las credenciales guardadas de Wallbit/IOL? El código de IOL Lab ya trata el fallo de descifrado como 'token muerto' y borra la fila (backend/main.py:31431-31436).
- El DDL de twr_periods.quality documenta 3 valores pero el motor escribe un cuarto, 'dudoso' (backend/twr.py:772), que es justamente el que hace que twr_de NO publique número. ¿Se actualiza el comentario o hay algún consumidor que asume los 3 originales?
- advisor_clients.consent_ref se escribe con la traza del consentimiento y NADIE la lee jamás. ¿Es para una auditoría manual futura, o quedó huérfana? Mismo caso con revoked_at, que es el dato que haría falta para el roster histórico del TWR (el sesgo de supervivencia que backend/advisor_twr.py:18-23 documenta como pendiente).
- No hay endpoint para cambiar advisor_clients.permission después de creado el vínculo (el PATCH sólo toca label/notes/phone). ¿Un asesor debería poder bajar a read-only un cliente managed sin revocarlo y volver a crearlo?
- GET /api/advisor/twr escribe en la base (sella meses vía twr.sellar dentro del handler, backend/main.py:33708-33710). ¿Se quiere mover el sellado a un cron, o es aceptable el efecto secundario en un GET?
- advisor_alerts.purge_old hace un DELETE GLOBAL sin advisor_uid, disparado desde la lectura del feed (backend/advisor_alerts.py:84,92-97): cualquier asesor que abra su panel purga los eventos de todos. ¿Intencional?
- ¿Es deliberado que `billing_events` sobreviva al borrado de cuenta con el raw_payload completo, o es un efecto colateral de que nadie escribe `user_id`? Si es lo segundo, es un agujero de derecho al olvido y se arregla con una línea en los dos INSERT.
- ¿El webhook de MercadoPago (POST /api/billing/webhook) sigue vivo por back-compat con suscriptores viejos, o se puede desmontar? De eso depende si el `$0` de amount_ars en los mails de bienvenida/recibo es un bug latente o uno activo.
- ¿`import_incidents` en la base de dev es residuo de una rama local tuya, o quedó también en producción? Si quedó en prod, hay una tabla sin lector participando del borrado de cuentas.
- ¿Querés que `import_raw_rows.raw_json` se conserve para siempre? Es el 92% de la base. Una política del tipo 'vaciar raw_json de batches confirmados con más de N meses' recorta el 90% del tamaño sin tocar nada que la app lea (la app lee raw_json solo en 3 lugares de diagnóstico: backend/main.py:17195, :18231, :31649).
- ¿Hay alguna razón por la que `news`, `financial_events` y `bond_indices_daily` guarden `fetched_at`/`updated_at` si la frescura la decide un dict en memoria? Mover el TTL a la columna arreglaría de una el re-fetch masivo de cada redeploy y el `stale` inconsistente entre workers.
- ¿Cuál es el criterio de retención que querés para login_history? Hoy es infinito con IP en claro. Una poda a 90 días conserva el valor de seguridad (detectar device nuevo) y saca el pasivo.
- En el modelo de bajas: `credit_ledger` se anonimiza y `trial_consumed` sobrevive (los dos por buenas razones documentadas), pero `plan_events` (telemetría de conversión) y `trial_email_log` se borran con la cuenta. ¿Es intencional que borrar la cuenta borre la telemetría de conversión de esa persona?
- ¿Querés que abra los tres crons de limpieza que los comentarios ya prometen (yfinance_cache > 7d, ai_analyses_cache vencidos, email_verification_codes > 30d)? Son tres jobs de 3 líneas cada uno y hoy las tres tablas crecen sin techo.
- El hallazgo #1 (asesor vencido conserva acceso a las carteras): ¿es una decisión de negocio deliberada — 'no le corto la relación con sus clientes por un mes impago' — o nunca se pensó? La corrección natural es una línea en `_resolve_client_context` o revocar/pausar los vínculos en `_downgrade_expired_credit`, y son dos productos distintos (cortar seco vs. dejar solo lectura).
- ¿`reportes.historicos` y `comportamiento.full` tienen que estar gateadas de verdad en el backend, o el paywall es intencionalmente 'de vidriera' (frena al 99% que usa la UI y no vale la fricción de cerrarlo)? Si es lo segundo, conviene escribirlo en ai/plan.py para que el próximo lector no lo tome por un bug.
- ¿Un asesor debería poder conectar la API key de Wallbit en la cuenta de un cliente RECLAMADO (cuenta independiente, con login propio)? Para un shadow tiene sentido; para un reclamado es un tercero operando credenciales de broker ajenas.
- Cuando el cliente acepta un pedido de acceso, el permiso que se concede es el que pidió el asesor (default `read_write`, backend/main.py:34811-34813). ¿La pantalla de aceptación deja claro que 'read_write' significa que el asesor puede CREAR Y BORRAR posiciones y operaciones en su cuenta, no solo mirar?
- ¿Vale la pena borrar `ai.hub` y `tax.helper` de FEATURE_IDS/PLAN_LIMITS, y `can_hub_query`/`record_hub_query` de quota.py? Hoy son 4 entradas por tier y 2 funciones que nadie llama; los tests validan que estén declaradas, así que el costo de sacarlas es tocar test_ai_plan.py.
- El comentario con el email del admin en claro (backend/main.py:125) anula el propósito del hash. ¿Se saca el comentario, o se acepta que el hash es solo para que no aparezca en un grep casual de emails?
- ¿Se planea alguna vez emitir un segundo email admin? Hoy `ADMIN_EMAIL_HASH` guarda UN hash y `_is_admin_email` compara contra uno solo — soportar dos admins pide cambiar la env var a lista.
- El plan del asesor no incluye a sus clientes: tras el claim el cliente cae a Free (backend/ai/quota.py:181-188). ¿El cliente se entera de eso en algún lado del flujo de claim, o descubre solo que perdió features cuando entra por primera vez a su cuenta?
- ¿'pppi' en ARS_BROKER_NAMES (backend/main.py:2835) es un typo de 'ppi'? Si lo es, todo broker PPI auto-creado se infiere USD en vez de ARS.
- El 403 de cuota de POST /api/brokers dice literalmente 'El plan Free permite 1 broker' aunque quien lo reciba sea un usuario Plus con brokers_max=3 (y con upgrade.available=false). ¿Es un mensaje que quedó viejo o hay una intención?
- ¿Es aceptable que DELETE /api/brokers/{bid} sobre un broker sin ops ni monthly borre la serie de snapshots ENTERA del usuario (backend/main.py:4519), incluida la de los demás brokers?
- POST /api/snapshots acepta el total_value calculado por el browser sin ninguna validación de plausibilidad más allá de ge=0, y esa serie es la base de rendimientos, máximos y comparación contra benchmark. ¿Hay algún clamp aguas abajo o esto es confianza total en el cliente?
- ¿Los tokens de password reset deberían guardarse hasheados? Hoy van en claro en password_reset_tokens (backend/main.py:3383-3387) mientras el User-Agent sí se hashea 'por si se filtra la DB'.
- DELETE /api/me borra en cascada todas las cuentas shadow que el usuario administra. ¿El asesor está avisado de eso en el flujo de UI antes de confirmar?
- ¿Cuántos workers corren en Railway? De eso depende si los rate limits de login/register/forgot-password valen lo que dicen valer.
- ¿El calendario macro hardcodeado (MACRO_EVENTS_CALENDAR, backend/main.py:5734-5788) tiene dueño y recordatorio para renovarse en enero 2027, o el tab 'Popular' se va a vaciar solo?
- ¿'UVA' y 'A3500' en la allowlist de /api/bond-indices son features planeadas o restos de un diseño que nunca se implementó? Hoy devuelven serie vacía con stale:true permanente.
- ¿Los dos endpoints de escritura del tramo (POST /api/positions, PATCH /api/positions/group) tenían que reintentar ante 'database is locked'? Porque el decorador está puesto pero no funciona — habría que sacar el `except Exception` o sacar el decorador para no dar falsa sensación de cobertura.
- El rename grupal cascadea a `operations` con el par de brokers pero sólo renombra los lotes del broker exacto. ¿Es intencional (el sibling se renombra aparte) o es el bug que sospecho?
- ¿Alguien depende de que /api/positions devuelva `undo_meta_json` y `user_id`? Si no, conviene proyectar columnas explícitas.
- Con el mercado cerrado, ¿está bien que la variación diaria de todos los .BA muestre 0 (backend/main.py:7917) en lugar del movimiento de la última rueda? Es una decisión de producto tanto como técnica.
- ¿Por qué /api/prices/history usa auto_adjust=False mientras /api/prices y prev-close usan True? El mini-chart y el precio de la tabla salen de series ajustadas distinto.
- ¿Cuántas cuentas ya cobraron un plazo fijo EN PESOS? Es la pregunta que decide si el bug de `Interés PF` (pesos contados como dólares en el P&L realizado y en el win-rate) es teórico o ya está mordiendo. El comentario de backend/realized_pnl.py:66 dice '0 filas' pero está fechado.
- El alta de un plazo fijo debita el cash del broker y el borrado no lo devuelve, porque `source_broker` no se guarda en ninguna parte. ¿Cuánta plata fantasma hay hoy por PFs dados de alta con origen y después borrados o editados?
- `POST /api/brokers/reconcile-cash` bookea el ajuste en el MES MÁS ANTIGUO del broker dolarizándolo con el `tc_blue` que manda el navegador (el de hoy). Para un broker con historia de 2015, ¿cuánto deforma eso el capital aportado? Es la misma pata que ya se arregló en el import y en `/api/cash/flow`.
- `POST /api/conversions` acepta `ars_amount`, `usd_amount` y `tc` sin chequear que sean coherentes entre sí. ¿Vale la pena una validación `abs(ars - usd*tc) / ars < ε` antes de que un cliente viejo o el chat de la IA mande una terna imposible y envenene el `tc_compra` promedio del cash dólar?
- El fallback legacy de amortización (usar `net_amount` como VN cuando no viene `face_amortized`) sigue vivo, protegido solo por un umbral `×1.5` que no dispara con muchos nominales. ¿Se puede exigir `face_amortized` cuando `broker_currency != currency` en vez de dejar el fallback?
- Hay filas de posición que NO se pueden borrar desde la UI (las manuales viejas sin `undo_meta_json` reciben un 400 bloqueante, backend/main.py:8639). ¿Cuántas hay y qué se le dice al usuario que quiere sacarlas?
- El campo `evidence` de `/split-check` siempre vale 'fecha' desde el audit del 2026-08-10, pero el comentario documenta un valor 'precio'. ¿El frontend todavía tiene una rama para 'precio'? Si sí, es código muerto de los dos lados.
- ¿Se decidió a propósito que los 5 endpoints que mueven plata NO invaliden el cache de IA, o es un olvido? Es una asimetría limpia contra los de posiciones y PF, que sí lo hacen.
- ¿El monthly del BROKER debería recibir el mismo P&L que 'global' cuando la venta es en USD sobre un broker ARS? Hoy el broker recibe 0 y 'global' el número real (backend/main.py:11388-11392). ¿Cuál de los dos es el que leen los Reportes por broker?
- En `POST /api/positions/sell`, ¿qué debería pasar cuando el body NO trae `tc_venta` en una venta ARS bajo FX v2: estampar el TC resuelto por `fx_for_date` (coherente con el P&L) o rechazar el request? Hoy estampa 1.0 y queda incoherente con el pnl_usd.
- El CSV `transactions.csv` que va al contador duplica los depósitos importados (los cuenta como fila de import y otra vez dentro del total mensual). ¿Ya se usó alguno de esos CSV para una declaración? Es el mismo tipo de riesgo que el comentario de operations.csv describe como irreversible.
- ¿`GET /api/monthly` tiene que seguir siendo el disparador del rollover mensual, o el rollover debería vivir en el cron? Hoy una cuenta que nadie abre no rolla, y una que se abre dos veces el día 1 depende del UNIQUE para no duplicar.
- El cap de 36 meses de `_rollover_to_current_month` (backend/main.py:11489) hace que una cuenta abandonada 4 años necesite DOS requests para ponerse al día, sin decirlo en la respuesta. ¿Debería devolver una señal de 'quedó incompleto'?
- Los endpoints `/api/insights/gap-month` y `/api/insights/mtm-audit` son herramientas de diagnóstico interno pero cualquier usuario autenticado los puede llamar sobre sus propios datos (payloads grandes, sin paginación, con `holdings_json` crudo). ¿Van gateados por admin o se dejan abiertos a propósito?
- `_build_movements` clasifica TODA operación manual como SELL/BUY. ¿Se quiere que Cupón, Dividendo, Amortización y Futuros tengan su propio `type` en Movimientos (y por lo tanto salgan del cálculo de comisiones embebidas)?
- Futuros en broker ARS: ¿es un caso que existe hoy en producción? Si hay usuarios con dólar futuro Rofex/Matba cargado a mano y después editado o borrado, el saldo de esa cuenta está descuadrado por ~tc. ¿Querés que se mida?
- `behavioral_tags_visible`: ¿el paywall de Comportamiento se pensó como visual (mostrar 3 de 12 con blur) o como restricción real de datos? Si es lo segundo, el endpoint tiene que filtrar antes de responder.
- `/api/wrapped/{year}` y `/api/behavioral/insights` no tienen gate de plan ni rate limit y disparan fetch de precios (y a veces un GET a argentinadatos con timeout 8s) dentro del request. ¿Es intencional que sean gratis y sin tope?
- `POST /api/admin/repair-user-history` es el único reparador sin dry-run y toca la cuenta de un tercero. ¿Querés un `apply=false` como los otros cuatro?
- En Supabase el dry-run de `repair-snapshots-all`, `backfill-recompute`, `backfill-mtm` y `backfill-currency` no existe (501). ¿Se están corriendo hoy en prod con `apply=true` a ciegas, o esos botones están de hecho apagados?
- `min_capital` de `backfill-currency` viaja como query param sin clamp. ¿Lo dejamos así o le ponemos un techo duro del lado del server?
- `_cagr_from_monthly_rows` está muerto: ¿se borra, o queda como fallback documentado para algo que todavía no se cableó?
- El diagnóstico de objetivos ignora `modo`/`moneda` y descarta el acumulado cuando el motor no anualiza. ¿Se alinea con `/api/goals/cagr` (pasarle los dos params y usar `total_return_pct` cuando `cagr` es None)?
- `DELETE /api/assets/history` borra el activo en TODOS los brokers. ¿La UI le avisa al usuario que el tacho de esa fila se lleva también las otras cuentas?
- La fórmula de reversión de cash está copiada a mano en 3 lugares para ser simétrica con el persister, y ya divergen en qué broker recibe la plata (`cash_broker_for` sí/no). ¿Vale unificarlas en un helper único antes de que se separen más?
- `ventas-legacy-debug` (backend/main.py:17401-17407) NO filtra `op_type='Venta'` pese al nombre, al docstring y a que las claves de salida se llaman `ventas_hoy_muestran_usd` / `ventas_pasarian_a_usd`. ¿Es intencional (querías medir todas las operaciones sin sellar) o el informe con el que se decidió activar la deducción estaba inflado por compras, dividendos y conversiones?
- `repair-comisiones` (backend/main.py:17706) hace `objetivo = objetivo[:limite]` ANTES de calcular `comisiones_falsas_total` y `usuarios`, sin reportar cuántas quedaron afuera — justo lo que `check-invariantes` declara inaceptable en backend/main.py:17792. Con más de 5.000 posiciones afectadas, ¿el número que viste al decidir aplicarlo era el real o el truncado?
- `repair-comisiones` elige el piso (USD 25 vs ARS 35.000) por `positions.currency`, la misma columna que otros dos endpoints del tramo marcan como no confiable (`suspect`). Una posición ARS mal marcada USD se compara contra un piso ~1400× más chico y se borra. ¿Se verificó eso antes de correrlo con apply=true?
- `diagnose-reportes-basis` en modo agregado llama `compute_metrics_for_period` sin `modo` ni `moneda` (backend/main.py:17880), o sea siempre certero/USD. El docstring promete medir 'el número que ESA cuenta está viendo hoy'. ¿Querés que también cubra estimado y pesos, que es donde vive buena parte del padrón desde `4d7b63e0`?
- El bloque `mediciones_dudosas` de `diagnose-reportes-basis` (backend/main.py:18059-18066) corre `twr.serie_medible` DOS veces por CADA usuario con snapshots, ignorando `limit_accounts`. ¿Ese endpoint llegó a correr alguna vez en producción sin timeout del proxy, o es el mismo pozo que ya te comió a `diagnose-sell-fx` v1 y a `fx-migrate-candidates` v1?
- Seis de los siete endpoints que leen `import_normalized_tx` NO filtran `excluded_at IS NULL`; sólo `diagnose-flujo-implausible` lo hace. ¿Es una decisión (querés ver también lo excluido) o los otros seis están contando filas que el usuario ya sacó a mano?
- `cleanup-future-snapshots` (backend/main.py:17344) borra snapshots de TODOS los usuarios en una sola transacción, sin `user_id`, sin backup y sin undo, con `apply` en el query string de un POST sin body. ¿Vale la pena pedir un token de confirmación o al menos acotarlo por cuenta?
- ¿Sabías que /api/admin/delete-snapshot y /api/admin/wipe-broker-data solo tocan TU cuenta y no la de un usuario objetivo? Si la intención era soporte sobre terceros, hoy no hay ningún endpoint que lo permita.
- El broadcast re-mailea a todos si cambiás una coma del asunto (la idempotencia es por hash del contenido, no por campaña). ¿Querés un campaign_id explícito, o alcanza con saberlo?
- ¿Alguna vez corriste re-engagement/gift-plan/broadcast con confirm=true y limit=0 (sin tope)? Es el default y no hay más red que el dry run.
- restore-tier no puede reparar una cuenta 'advisor' aunque grant-comp sí la pueda otorgar. ¿Te pasó de tener que arreglar un asesor a mano en la base?
- ¿Es intencional que restore-tier y grant-comp reseteen quota_window_from? Un usuario al que le restaurás el tier pierde/reinicia su ventana de cuota de IA como efecto colateral no documentado.
- /api/admin/billing/inspect devuelve payloads crudos de webhooks de pago y el docstring invita a copiarlos afuera para diagnosticar. ¿Te sirve que trunque/redacte los campos sensibles, o lo usás así a propósito?
- Hay 7 endpoints vivos sin ninguna UI que los llame (disk-usage, delete-snapshot, ai/tool-usage, billing/inspect, fci/probe, pf/probe, wipe-broker-data). ¿Los usás por curl/browser o son candidatos a borrar?
- trial-invite hace ~5 queries por usuario elegible incluso en dry run. ¿Cuántos usuarios sin avisar hay hoy? Si son miles, el dry run ya debe estar tardando.
- El DELETE de usuario devuelve el mensaje crudo de la excepción al cliente. ¿Querés que lo loguee y devuelva un 500 genérico?
- backup-trigger corre el dump completo síncrono en el request. ¿Alguna vez te devolvió respuesta, o siempre te corta el gateway y tenés que mirar los logs?
- El plan Asesor: cuando un asesor entra a un cliente Free y pide un analisis, ¿de que cupo deberia descontar? quota.py:74 dice que del pool del asesor, main.py:26080 descuenta del cliente. ¿Cual de las dos es la decision de producto real?
- ¿Los usuarios Free tienen 1 analisis por semana a proposito? LIMITS lo fija en 1 (backend/ai/quota.py:72) pero toda la copy de upgrade del endpoint esta escrita como si fueran 6.
- ¿El techo de _TRADE_MAX_NOTIONAL (US$5M / $5.000M, backend/main.py:23244) es un tope de sanidad o hay usuarios reales cerca de el?
- ¿Hay planes de correr uvicorn con --workers o mas de una replica? Si la respuesta es si, el flujo de registro por chat y la ventana de undo de 24h necesitan persistencia antes de ese cambio.
- El 400 de topic desconocido devuelve la lista completa de los ~40 topics internos. ¿Es intencional (util para debug del frontend) o conviene recortarlo?
- ¿Por que register_trade esta habilitado para Free pero get_fx_rates no? ¿Es una decision de costo o quedo asi por omision al armar la whitelist?
- `POST /api/billing/sync` sigue llamándose desde `frontend/src/pages/BillingReturn.jsx` pero consulta MercadoPago con ids de Rebill. ¿Qué ve hoy un usuario que acaba de pagar y cae en /billing/success: un 502, un spinner infinito, o el frontend ignora el error y espera al webhook? No pude ejecutarlo.
- `MP_WEBHOOK_SECRET` está seteada en Railway hoy? De eso depende si el fail-open de `backend/main.py:27554-27560` es teórico o vivo. No leí `.env` ni el panel.
- ¿Cuántos workers/instancias corren en producción? Todo el flujo de registro conversacional del chat (`_TRADE_DRAFT` en RAM, `backend/main.py:23228`) asume un solo proceso. Con más de uno, la confirmación del 'sí' se pierde de forma intermitente y difícil de reproducir.
- El `GET /api/billing/status` no lo llama nadie en el frontend. ¿Se puede borrar, o hay algún cliente externo (script, panel, Postman) que dependa de él?
- El bloque `_AI_CHAT_SYSTEM_ADVISOR_IN_CLIENT` hoy solo llega cuando el cliente es free/plus (por el bug de la conexión cerrada). ¿La intención era que un asesor dentro de la cuenta de un cliente Pro también lo reciba? Si sí, arreglarlo cambia el comportamiento del prompt para esos casos.
- `_ALLOWED_PLAN_EVENTS` tiene 4 nombres y el frontend puede estar mandando más. ¿Vale la pena loguear los descartados, aunque sea a WARN, para no perder métricas en silencio?
- Los dominios `*.rebill.dev` (sandbox) están en la allowlist de URLs de pago también en prod (`backend/main.py:26594-26600`). ¿Se pueden separar por entorno?
- La rama `else` de `backend/main.py:30125-30170` es inalcanzable (la condición de 29891 cubre los 7 formatos posibles). ¿Se puede borrar, o quedó ahí como fallback previsto para un formato futuro? Son ~45 líneas que incluyen el ÚNICO camino de gap-fill puro sin override.
- `/api/imports/*` y `/api/wallbit/*` heredan `X-Rendi-Client-Id`: un asesor con `read_write` puede importar archivos y conectar/desconectar credenciales de broker en la cuenta de un cliente. ¿Es intencional? Sólo `/tenencia/preview` distingue el contexto (para `motivo_corte` y `gate_over`); `/confirm`, `/preview` y Wallbit no.
- `_wallbit_reconcile_positions` llama a `_tenencia_apply_override` con `complete=True` y `gate_over` en su default `False` (`backend/main.py:31152-31154`): cada sync de Wallbit puede CERRAR posiciones sin pedir aprobación, ni siquiera en contexto de asesor. ¿Se decidió así a propósito, o quedó desalineado con el gate que sí se agregó al path de fotos?
- `GET /api/iol/lab/status` hace una llamada de red a IOL y escribe/borra credenciales (renovación oportunista, `backend/main.py:31475-31481`). ¿Vale mantener eso en un GET, o conviene moverlo al cron / a `POST /api/iol/lab/refresh`?
- Dos endpoints devuelven el texto crudo de la excepción interna al cliente (`backend/main.py:30528` y `30994`). ¿Se puede reemplazar por un id de error + log?
- `/api/imports/classify-tenencia` no tiene cap de cantidad de archivos y, cuando uno excede los 5 MB, procesa los bytes truncados en vez de devolver 400 (`backend/main.py:29509-29520`). ¿Alinearlo con `/preview` (tope 20 + 400)?
- El rate limit de `/api/feedback/recommendation` es por IP SIN uid (`suffix="recommendation"`, `backend/main.py:29218`): 5/hora compartidos por toda una oficina o carrier. ¿Se quiso así o es un olvido respecto del patrón `suffix=f"...:{uid}"` del resto?
- `GET /api/imports/mappings` (backend/main.py:31665) está tapado por `GET /api/imports/{batch_id}` (31637) y el wizard se come el 404 con un `.catch` — ¿alguien vio alguna vez una plantilla de mapeo guardada en producción, o el feature entero está muerto desde que se escribió?
- `POST /api/imports/wipe-broker` (31740) borra posiciones, operaciones, mensuales, snapshots y la fila del broker, y solo pide sesión. ¿Es deliberado que no sea admin-only ni tenga confirmación server-side, sabiendo que un asesor con `read_write` lo dispara sobre la cuenta del cliente?
- `GET /api/reports/timeline` y `/period` hacen dos llamadas de red sin caché por request (argentinadatos + yfinance `period="max"`). Ya existe `_bench_cache` en el archivo pero estos call-sites no lo usan. ¿Fue una decisión o se perdió en un refactor?
- `ADVISOR_BRIEF_TOKEN` cae a `SNAPSHOT_CRON_TOKEN` como fallback (33946-33947): el token del cron de snapshots también dispara mails a todo el padrón de asesores. ¿Los dos secretos están efectivamente seteados en Railway, o hoy hay uno solo haciendo las dos cosas?
- Las prefs del brief son opt-in por default (34005-34006: sin fila en `advisor_profile` devuelve True/True). ¿Un asesor nuevo tiene que recibir dos mails por día hábil sin haberlos pedido?
- `_migrate_estampar_base` (32166) no tiene endpoint para forzarlo — el propio docstring dice que la única palanca es un redeploy, y que sin estampar salen alertas de drawdown de −492.864% POR MAIL. ¿Vale la pena un endpoint admin, o el `_run_with_lock_retry(attempts=5)` ya alcanzó?
- `AlertUpdateIn` (33394) no valida `direction`/`channel`/`repeat` contra su enum mientras que `AlertCreateIn` sí. ¿Es olvido o hay algún cliente que manda valores libres?
- `cash_value` de `/api/reports/period` publica pesos rotulados como dólares y el comentario (32833-32852) dice que se deja así porque nadie lo consume. ¿Se borra la clave del payload o se arregla valuando por `_native_ccy`?
- ¿El Δ7d de `/api/advisor/book/detail` tiene que cerrar exacto con el del hero (como promete su docstring en backend/main.py:37948) o se aceptó que diverja? Hoy divergen por dos motivos verificados: zona horaria del "hoy" y el piso de antigüedad `_floor7`.
- El gráfico de `/book/history` dibuja el AUM sin filtrar por `_es_base_de_mercado`. ¿Es intencional (mostrar un valor ≠ restar dos valores, como el comentario justifica para el AUM del hero) o es el mismo escalón import-vs-mercado ahora dibujado como línea?
- `GET /advisor/book/asset-clients` recomputa el libro entero por click. ¿Vale la pena cachear `_advisor_positions_valued` + `_advisor_realized_raw` por asesor con un TTL corto, o el volumen actual de asesores no lo justifica todavía?
- El link público de informe (`/api/reports/public/{token}`) no registra aperturas. ¿Interesa saber si el cliente lo abrió (métrica de producto) o la ausencia de log es deliberada por privacidad?
- `_advisor_group_op_apply` devuelve 400 sin las razones cuando ninguna fila es válida. ¿Se puede cambiar a devolver 200 con `applied: []` + `skipped` poblado, o hay algún consumidor que dependa del 400?
- ¿El block trade va a tener alguna vez venta grupal? `has_asset` y el `op_kind` de la tabla están puestos para eso pero no hay código que los use.