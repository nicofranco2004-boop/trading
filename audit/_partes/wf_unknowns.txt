- El valor real de las variables de entorno en el dashboard de Railway. Desde el código sólo se puede afirmar CÓMO se elige el motor (backend/main.py:423-424), no qué está seteado hoy. La confirmación se haría entrando a Railway o pegándole a /api/admin/disk-usage (si devuelve db_internals con page_size/file_mb, es SQLite).
- Si el scheduler in-process (backend/main.py:32524-32575) y los crons externos (SNAPSHOT_CRON_TOKEN, ALERTS_CRON_TOKEN, ADVISOR_BRIEF_TOKEN, IOL_LAB_CRON_TOKEN) pueden disparar el mismo job dos veces el mismo día. Existen los dos caminos; no rastreé si hay guard de idempotencia.
- Cuál de los tres conteos de tablas es el correcto: 58 (backend/pgshim.py:480,907,935), 60 (backend/scripts/copiar_a_postgres.py:10, backend/scripts/mkschema.py:46) o 63 (grep -c '^CREATE TABLE' backend/schema_pg.sql).
- Si `pytest` corrido desde la raíz del repo levanta o no backend/pytest.ini (y por lo tanto si la protección contra scripts/test_emails.py aplica). Inferido del algoritmo de rootdir de pytest; no lo ejecuté porque el brief prohíbe correr la suite.
- Cuánto del código de MIGRACION_POSTGRES.md sigue siendo cierto: el documento dice 'rama spike/postgres, NO se deploya' (:24-25) y ese código HOY está en main. La fecha de la última actualización real del .md no la pude determinar sin git.
- No encontrado: ningún archivo CORRECTNESS_AUDIT_2026-06-25.md en el repo, pese a estar citado 11 veces.
- No encontrado: configuración de CI (no hay .github/, .gitlab-ci.yml, Dockerfile, Makefile ni pre-commit).
- No encontrado: ningún tsconfig.json, .eslintrc* ni configuración de Prettier en frontend/.
- No pude determinar si producción corre hoy SQLite o Postgres: `USANDO_PG = bool(DATABASE_URL)` (backend/main.py:423-424) y la variable de entorno no es legible desde el código. Documenté las DOS ramas.
- `positions.notes`: no encontré ningún lector en el frontend (grep en Cartera.jsx / PositionRow.jsx sin resultados). Puede haber un consumo indirecto vía `SELECT *` que no rastreé componente por componente.
- El id explícito que `/api/sections/restore` re-inserta en `positions` (backend/main.py:31871-31874) es seguro en SQLite (AUTOINCREMENT no reusa ids). En Postgres, `GENERATED BY DEFAULT AS IDENTITY` acepta el id pero no adelanta la secuencia; no encontré ningún `setval` ni manejo de esa colisión potencial — no pude verificar si alguien ya lo pensó.
- `operations.date` es TEXT libre; sé que hay filas mal formadas porque el código las filtra con `date LIKE '____-__-__%'` (backend/main.py:9455-9463, 9475), pero no sé cuántas ni de qué caminos salieron (no consulté datos de usuarios).
- No verifiqué el contenido real de `archived_positions.payload` ni de `deleted_ops_journal.payload_json` en la base (regla de no hacer SELECT de datos de usuarios): la forma de cada JSON la deduje del código que los escribe.
- `users.gift_plus_email_sent_at` existe en la base de dev pero no en el código actual. No pude confirmar si sigue existiendo en producción (la base de dev viene de una rama vieja).
- Volumen real de filas de las 23 tablas: la copia de la base de dev (backend/trading.db) sólo contiene asset_last_price de todo este alcance — viene de una rama vieja y no sirve como evidencia secundaria
- Si IOL_LAB_EMAILS está seteada en producción (sólo verifiqué que backend/main.py:31340 la lee; no leí .env)
- Si REPORTS_TTL_DAYS está seteada (default 180 en backend/main.py:36047)
- Si ALERTS_CRON_TOKEN está seteada: sin ella POST /api/alerts/evaluate devuelve 503 y NI las alertas de precio NI las del libro se evalúan nunca (backend/main.py:33597-33599)
- Si advisor_alert_state / advisor_alert_events / broadcast_send_log / alert_symbol_state acumulan filas huérfanas HOY (la lógica dice que sí, pero no consulté datos de usuarios)
- Cuántas cuentas tienen credenciales de broker guardadas en user_broker_credentials (no hice SELECT de datos de usuarios)
- Si ledger_replay/price_history alguna vez estuvieron conectados y se desconectaron, o nunca se conectaron: sólo verifiqué el estado actual del código en b74f450f
- No pude confirmar qué contiene realmente `billing_events.raw_payload` (si trae email del pagador u otra PII): sería necesario ver un payload real de Rebill/MP, y no hay fixtures de payload en el repo. La afirmación de riesgo de privacidad queda marcada como [I].
- No verifiqué contra el historial de git de dónde salió `import_incidents` (la regla read-only excluye correr git). La atribución al 'Import Guardian' es inferencia sobre la forma de las columnas (new_codes_json, diagnosis_text, diagnosis_model, notified_at, reviewed_by).
- No medí el volumen real en producción de `news`, `bond_indices_daily` ni `login_history`. Los únicos números duros que tengo son los de la copia de dev (news 2179, fx_rates_daily 5634, bond_indices_daily 0, financial_events 86, yfinance_cache 14, asset_last_price 89, fci_catalog/fci_prices 117) y los que el propio repo declara medidos (3,1M en import_raw_rows, 933 MB, ~1084 usuarios).
- No auditué todos los callers de `apply_last_known_prices` para saber en qué moneda queda cada fila de `asset_last_price`. Solo consta que hay al menos un caller que convierte a pesos ANTES de persistir (backend/main.py:7821), lo que hace que la semántica de la columna `price` dependa del camino.
- No sé si prod corre hoy sobre SQLite o Postgres: depende de la env var DATABASE_URL (backend/main.py:424) y no leí .env. Toda la sección §0 (las tres purgas que no corren en PG) es condicional a eso.
- No verifiqué si el endpoint POST /api/billing/webhook (MercadoPago, backend/main.py:27501) sigue recibiendo tráfico real, lo que decide si el bug de amount_ars=0 en los mails es activo o puramente latente.
- `plan_events.props_json` se escribe y no encontré ningún lector; no descarté del todo que lo consuma algún dashboard externo por SQL directo.
- Cuántas filas de `users` tienen `password_changed_at` en NULL en producción (el agujero de invalidación de sesión del punto 4). No hice SELECT de datos de usuarios y la trading.db del repo viene de una rama vieja.
- Si en producción corre SQLite o Postgres. El código soporta las dos vías por el interruptor `DATABASE_URL` (backend/main.py:423-424) y el esquema PG sí trae default en `password_changed_at` (backend/schema_pg.sql:1725, 1760), lo que cerraría el agujero — pero no puedo saber cuál está activo sin mirar las env vars.
- Si Railway corre una o más réplicas del proceso. El comando de arranque es un uvicorn sin --workers (nixpacks.toml:31), pero el escalado horizontal es config de Railway, no del repo. De eso depende si el rate limiting in-memory sigue siendo efectivo.
- Cuántos asesores hay hoy con el grant vencido y vínculos `advisor_clients` todavía en status='active' (o sea, el tamaño real del hallazgo #1).
- Si `RENDI_TRUSTED_PROXY_HOPS` está seteada en prod. Sin ella, `_ip_del_cliente` (backend/main.py:369-387) elige la última IP pública de la cadena X-Forwarded-For, que es una heurística — con el número de saltos correcto sería determinista.
- Si `TRIALS_MONTHLY_CAP` está seteada. Con 0 (default) el tope no corre y el ledger vuelve a ser solo auditoría; el enforcement atómico del tope solo se activa con cap > 0 (backend/billing/trial.py:367-390).
- El valor de `REPORTS_TTL_DAYS` en prod (default 180 días) — decide cuánto tiempo queda legible por URL la cartera de un cliente en un informe reenviado por WhatsApp.
- Si `ADMIN_EMAIL_HASH` está overrideada por env var en prod, o si corre el hash hardcodeado de backend/main.py:126.
- No verifiqué qué índices existen sobre (user_id, broker) en las 6 tablas de NAME_KEYED_TABLES, así que el costo real del cascade de PUT /api/brokers/{bid} sobre import_normalized_tx queda como inferencia
- No verifiqué si register() puede realmente sufrir un OperationalError DESPUÉS del background_tasks.add_task (lo que duplicaría el mail de verificación bajo @reintentar_si_trabada)
- No verifiqué si en producción corre más de un worker/instancia: de eso depende que el rate limiting in-memory y el _reset_progress in-memory funcionen o no
- No verifiqué el comportamiento real de POST /api/snapshots contra una base sin las columnas base/apto (solo que el INSERT las nombra sin guard mientras el GET sí guarda)
- No verifiqué si 'pppi' en ARS_BROKER_NAMES es un typo de 'ppi' o un nombre histórico intencional
- No verifiqué si password_reset_tokens.token tiene índice único ni si hay algún hashing aguas abajo (el INSERT guarda el token tal cual)
- No pude confirmar si en la práctica dos usuarios distintos tienen valores de CCL distintos (_display_ccl, backend/main.py:4904). De eso depende si el cache sin uid de /api/prices/history (cache_key = sym:period, backend/main.py:8071) realmente filtra la serie en pesos de un usuario a otro.
- No verifiqué si existe un UNIQUE sobre (user_id, broker, is_cash) en `positions`. Del esquema (backend/schema_pg.sql, CREATE TABLE positions) no surge ninguno, pero no revisé todos los CREATE INDEX/CONSTRAINT del archivo. De eso depende si POST /api/positions con is_cash=true puede dejar dos filas de cash para el mismo broker.
- No corrí nada: no pude medir cuánto tarda realmente el refresh bloqueante de /api/events/popular (20 tickers seriales contra yfinance) ni el cold start de /api/benchmarks. Los 15-20s son lo que dice el docstring (backend/main.py:5513-5518), no una medición mía.
- No pude verificar cuántas filas tiene realmente la serie CER en `bond_indices_daily` en producción (la base de dev del scratchpad viene de una rama vieja y no consulté datos). El tamaño del upsert de backend/main.py:5608-5618 y del JSON sin paginar es inferencia sobre el rango histórico de la fuente.
- No revisé los consumidores en el frontend: no sé qué campos de /api/positions (que devuelve SELECT *) se usan realmente, ni si algún caller itera Object.keys(prices) y se rompería con la clave mágica __meta de /api/prices.
- No verifiqué la implementación de ai.builders.dashboard_top_holdings.holding_weights (backend/main.py:7044): no sé qué tablas lee ni cuánto cuesta por request de /api/news/portfolio.
- No revisé _undo_edit_position_group más allá de su docstring (empieza en backend/main.py:8477, fuera del rango): no puedo afirmar si la reversión del rename de `operations` funciona correctamente con lo que el payload guarda.
- Quién consume los plazos fijos CERRADOS. `cobrar_plazo_fijo` deja la fila con `closed_at` y su docstring dice 'el PF cerrado retiene sus datos para las métricas' (backend/main.py:9280), pero `GET /api/plazos-fijos` filtra `closed_at IS NULL` sin parámetro para levantarlo y no hay otro endpoint de PF en el tramo. Puede que el lector esté fuera del rango 8500-11000, o que no exista.
- Si `renovacion_auto` lo dispara algo automáticamente. La columna se guarda (backend/main.py:9151) pero dentro del rango auditado el único camino que renueva es el POST manual. No barrí el archivo entero buscando un cron o job.
- El modelo `PositionIn` que consume `PUT /api/positions/{pid}` está definido fuera del rango; solo pude enumerar los campos que el handler efectivamente usa, no sus validadores.
- Qué tablas toca exactamente `_undo_edit_position_group` (backend/main.py:8477) — está fuera del rango auditado, así que el endpoint `POST /api/positions/group/undo/{token}` quedó documentado por delegación.
- Cómo usa el frontend el campo `evidence` de `/split-check` y el `logo` (URL de tercero, sin validar esquema) de `/api/pf/banks`. No leí el frontend.
- El detalle de las cascadas `_delete_position_cascade` (backend/main.py:14599) y `_delete_manual_position_cascade` (backend/main.py:14174), que son lo que realmente hace `DELETE /api/positions/{pid}`. Están 5.500 líneas más abajo, fuera del rango.
- Cuántos usuarios en producción tienen plazos fijos en pesos ya cobrados. `backend/realized_pnl.py:66` dice '0 filas en producción' pero es un comentario fechado, no una medición de hoy; no hago SELECT de datos de usuarios.
- No pude determinar si `PUT /api/futures/{fid}` (backend/main.py:12263) tiene algún consumidor fuera del frontend web (app móvil, scripts, integraciones): en frontend/src no hay ningún `api.put` a futuros — sólo GET/POST/DELETE/close en frontend/src/components/FuturosGroup.jsx.
- No medí el costo real de los `NOT IN (SELECT ...)` sobre `import_op_links` (backend/main.py:12465-12468 y :12758-12761): marqué la degradación como [I] a partir de la forma de la query, sin EXPLAIN QUERY PLAN ni datos de producción (la regla del audit prohíbe correr la app).
- No pude confirmar cuántos usuarios en producción tienen ventas ARS con `operations.fx_to_usd = 1.0` (el síntoma del hallazgo de backend/main.py:11310) — requeriría SELECT sobre datos de usuarios, que está prohibido.
- No verifiqué si existe alguna migración o job que rellene retroactivamente `manual_deposits_native`/`manual_withdrawals_native` para las filas creadas por el form de /mensual; sólo constaté los 2 escritores en backend/main.py:10007 y la migración de columna en backend/main.py:1109-1110.
- No rastreé quién consume `/api/insights/gap-month` y `/api/insights/mtm-audit` fuera de frontend/src/pages/Admin.jsx:1668,:1679 (por ejemplo, scripts de diagnóstico del founder).
- No pude establecer si el broker especial 'global' está protegido en algún lado contra que un usuario cree un broker real llamado literalmente 'global' — POST /api/monthly (backend/main.py:11979) sólo saltea la validación de existencia para ese nombre.
- No leí `twr.curva_indexada` ni `twr._aportado_por_punto`: qué tablas toca exactamente `/api/goals/cagr` (y por lo tanto `goal_diagnostic`) queda inferido del nombre y del docstring, no verificado.
- No leí `importing/recompute_backfill.py`, `scripts/backfill_historical_mtm.py` (más allá de grepear yfinance/data912) ni `scripts/backfill_currency_fix.py`: las tablas que escriben esos tres backfills salen del docstring del endpoint y de los kwargs que se les pasan, no de leer su cuerpo.
- No leí `importing/rebuild.py` (`rebuild_pair_asset`, `_is_safe_to_rebuild`) ni `importing/persister.py` (`broker_pair`, `cash_broker_for`, `reconciled_unit_price`, `_backfill_snapshots_from_monthly`): la simetría 'byte a byte' entre el reverso de cash y el persister la afirmo desde los comentarios del código de borrado, no verificándola contra el persister.
- No leí `snapshots_job.compute_net_deposited_db` ni `import_maturity.sweep_bond_amortizations`.
- No verifiqué si la asimetría de moneda en futuros tiene datos reales afectados en producción: no hice ningún SELECT (regla de la auditoría) y la base de dev del scratchpad viene de una rama vieja.
- No verifiqué qué `operation_type` usa cada parser para amortizaciones: asumí DIVIDEND/INTEREST porque el persister los trata así (backend/importing/persister.py:905), lo que hace que el guard de re-import del undo (backend/main.py:15075-15080) los cubra. Si algún parser emite otro tipo, ese guard tiene un agujero.
- No revisé si algún test cubre el ciclo alta→borrado de un futuro en un broker ARS (que es donde la asimetría de moneda se hace visible).
- Si `HAVING n > 2` con alias del SELECT (backend/main.py:16521-16528, en diagnose-sell-fx) sobrevive en Postgres: backend/pgshim.py sólo traduce strftime, sqlite_master, PRAGMA table_info, rowid e INSERT OR REPLACE, y no vi manejo de alias en HAVING. Si no lo traduce, ese bloque cae al except y se reporta como flujos_ars.error en vez de como dato.
- Qué devuelven exactamente `scripts.pg_type_audit.auditar` (backend/scripts/pg_type_audit.py:54) e `importing.invariantes.correr` (backend/importing/invariantes.py:281): main.py los pasa tal cual, así que la forma del JSON de /api/admin/pg-type-audit y /api/admin/check-invariantes no está declarada en el tramo auditado.
- Si `users.is_admin` está protegido contra escritura desde algún endpoint no-admin. get_admin_user (backend/main.py:2721) sólo lee esa columna; no audité quién la escribe.
- Cuántas cuentas afecta hoy cada diagnóstico: no ejecuté nada contra la base (regla read-only). Todos los números que aparecen en el informe son los que están escritos en los docstrings del propio código, no mediciones mías.
- Si `frontend/src/pages/Admin.jsx` gatea la UI de estos 5 endpoints con algún chequeo de rol propio, o si simplemente confía en el 403 del backend: no audité ese archivo más allá de los call sites.
- No leí backend/pricing/fci.py más allá de las firmas: no sé qué tablas escribe bootstrap() (usado por POST /api/admin/fci/refresh) ni qué shape exacto devuelve list_catalog() (que es literalmente la respuesta de GET /api/fci/catalog).
- No verifiqué si broadcast_send_log tiene índice/constraint UNIQUE sobre (content_hash, email). El código usa INSERT OR IGNORE (backend/main.py:19532), que solo es idempotente si existe esa constraint. La tabla se crea en backend/main.py:1937 y backend/schema_pg.sql:652 — no leí esas definiciones.
- No verifiqué si el `diagnosis` de /api/admin/billing/inspect (backend/main.py:19655-19667) coincide HOY con la lógica real de _downgrade_expired_credit / _downgrade_expired_cancellations del cron. Solo constaté que es una reimplementación manual, no una llamada a la función.
- No confirmé si el timeout de ~30s del rewrite de Vercel (documentado en backend/main.py:20366-20368 para el path de IA) aplica también a los endpoints /api/admin. Es lo que asumí al marcar backup-trigger y fci/refresh como riesgo de timeout.
- No verifiqué si el frontend gatea la ruta /admin además del gate del backend, ni si algún endpoint 'sin frontend' se llama desde scripts, crons o un panel externo que no esté en frontend/src.
- No leí billing/emails.py más allá de las firmas: no sé si send_custom escapa el body del broadcast al meterlo en el template HTML branded (backend/billing/emails.py:684), lo cual importa porque es texto libre de 20.000 chars.
- No sé si pgshim traduce correctamente `INSERT OR IGNORE` (backend/main.py:19532) ni `date('now','localtime')` (19779, 19929) — verifiqué que traduce datetime('now', '-N days') (backend/pgshim.py:157) pero no estos dos casos puntuales.
- No pude verificar broadcast_send_log en la copia de la base de desarrollo: la tabla no existe ahí (esa DB viene de una rama vieja), así que la evidencia es solo del código.
- Si _resolve_client_context (referenciado en backend/main.py:2819) valida que el asesor tenga el cliente efectivamente reclamado, o si basta con mandar un header con un uid arbitrario. No lei su cuerpo — esta fuera del rango asignado y decide el uid con el que /api/ai/analyze lee y escribe.
- El cuerpo completo de _register_trade_handler (backend/main.py:23460-24237, ~780 lineas): lei la firma, el docstring, las llamadas a _num_or_none/_trade_market_price/_execute_confirmed_trade y el payload final, pero no cada rama de validacion. Puede haber caminos de escritura que no pasen por el confirm_signal='yes' que documenta el docstring.
- El cuerpo completo de _execute_ai_tool_inner (backend/main.py:25240-25842, ~600 lineas): verifique el dispatch de las 13 ramas y lei entera la rama yfinance y la de remember_user_fact, pero no el interior de get_current_prices, get_market_news, get_recent_news_for_assets ni get_ar_bond_metadata.
- Si el frontend lee el 'tier' top-level o el 'usage.tier' de la respuesta de /api/ai/analyze — de eso depende si la divergencia de los dos tier es visible para el usuario o solo interna.
- Si existe algun cron o job que limpie yfinance_cache mas alla del TTL logico; solo vi el DELETE puntual de backend/main.py:2668 (tickers .BA) y el cleanup_expired de backend/ai/cache.py:174, que es de ai_analyses_cache, no de yfinance_cache.
- Cuantas replicas corre Railway hoy. railway.toml existe pero no lo abri; la conclusion de 'un solo proceso' sale de nixpacks.toml [start] (uvicorn sin --workers), que no dice nada del numero de instancias.
- No pude determinar el TTL ni el esquema real de la tabla de cache de IA: `ai.cache.get_cached` / `set_cached` / `invalidate_for_user` viven en `backend/ai/cache.py`, fuera del rango asignado.
- No pude determinar la forma exacta del dict que devuelve `ai.quota.get_current_usage` — lo inferí de los campos que consumen `_chat_quota_429` (backend/main.py:28190-28195) y el 429 de análisis (:26237). No leí `backend/ai/quota.py` completo.
- No pude determinar el contrato de `_build_fundamentals_response` (backend/main.py:22063) ni de `_build_fundamentals_ai_packet` (:22652): están fuera del rango.
- No pude determinar qué escribe exactamente `_execute_ai_tool` (backend/main.py:25209) en la cartera. Solo verifiqué los nombres de las tools con write-path (`register_trade`, `register_group_op`, `undo_last_trade`, `remember_user_fact`) y que el gating real es `allowed_names` en `_ai_chat_exec_tools` (:28040).
- No pude determinar el shape de `_advisor_book_chat_context` (definido en backend/main.py:35425, fuera del rango) más allá de las claves que el prompt del sistema menciona (`aum`, `clients`, `exposure`, `star`, `queues`, `distribution`).
- No pude determinar si en producción hay `MP_ACCESS_TOKEN` / `MP_WEBHOOK_SECRET` configuradas — no leí `.env` (regla de secretos). El comportamiento fail-open/fail-closed del webhook de MP depende de eso.
- No pude determinar si `credit_ledger`, `trial_consumed`, `trial_email_log` y `plan_events` existen en el `trading.db` de desarrollo — no consulté la base, y de todos modos el propio brief avisa que viene de una rama vieja.
- No pude verificar EJECUTANDO ninguno de los hallazgos (regla read-only, sin correr la app ni los tests): el bug de la conexión cerrada en :28412 y el 502 de `/api/billing/sync` están razonados leyendo el código y las funciones que llaman, no observados en runtime.
- No pude determinar cuántos procesos/workers corre el deploy, que es lo que decide si los dicts de módulo `_TRADE_DRAFT` / `_GROUP_DRAFT` y el `_rate_store` son correctos o intermitentemente rotos.
- No pude determinar si `mercadopago.parse_external_reference_full` tolera los `external_reference` que hoy escribe el path de Rebill (`rendi-{uid}-{plan}-{period}`) — está en `backend/billing/mercadopago.py`, no lo leí.
- Si `get_parser(format)` normaliza alias/mayúsculas: no lo verifiqué, y de eso depende si el `format` que se interpola en el header Content-Disposition de `backend/main.py:29446` puede traer caracteres arbitrarios del cliente.
- Completitud de `PII_KEYS` en `backend/iol_api.py` — verifiqué que `mask()` existe y enmascara `numero` cuando es string, pero no audité qué campos cubre la lista.
- Si existe gating de imports por plan/tier fuera del rango 29000-31500 (middleware, frontend, o un decorator): ningún endpoint de /api/imports/* del tramo chequea cuota.
- Límite real de variables de SQLite/pgshim para el `IN (...)` de `skip_row_indices` en `backend/main.py:30791-30798` — no probé cuántos índices hacen falta para romperlo.
- Forma exacta del payload de `parser_options()` y `parser_options_grouped()` (`backend/importing/pipeline.py:1277`, `1304`): sólo verifiqué que devuelven listas de dicts, no los campos.
- Si el server de producción corre single-process o con réplicas — todos los locks del tramo (`_wallbit_sync_locks`, `_iol_lab_runs_lock`, `_rate_store`) asumen single-process y el comentario de `backend/main.py:31009` lo declara, pero no lo pude verificar contra la config de deploy.
- Qué tablas exactas toca `run_daily_snapshot` (backend/snapshots_job.py) — no leí el módulo entero, solo lo llamé desde `/api/snapshots/run-cron` (backend/main.py:33647)
- La forma exacta del JSON que devuelve `_import_persister.revert_batch` (lo devuelve tal cual `POST /api/imports/{batch_id}/revert`, backend/main.py:31890) — no leí backend/importing/persister.py
- Qué servicios externos toca `_import_pipeline.run_preview` cuando `POST /api/imports/{batch_id}/redo` lo re-corre (backend/main.py:31967)
- La forma exacta del dict de `advisor_twr.salud_del_libro` que devuelve `GET /api/advisor/data-health` (backend/main.py:33715) — solo verifiqué la firma en backend/advisor_twr.py:32
- Si `home.market.get_movers` devuelve las claves `gainers`/`losers`: inferido de la firma `Dict[str, List[...]]` en backend/home/market.py:476, no leí `_build_movers`
- Cuántos workers/réplicas corre Railway: de eso depende si el scheduler in-process (backend/main.py:32095) duplica los 5 jobs y si los flags anti-doble-corrida sirven para algo
- Si la base de producción es SQLite o Postgres hoy: el código pasa por el shim `backend/pgshim.py` según la env var DATABASE_URL, y la copia de dev del repo es de una rama vieja (no tiene ni `alerts` ni `advisor_*`)
- Si `_advisor_report_payload` (backend/main.py:35615) hace fetches de red o toca servicios externos: está fuera del rango 36000-38100 y no lo leí. Lo que sí verifiqué es que `GET /api/reports/public/{token}` NO lo llama (sirve el payload congelado desde la DB).
- El comportamiento real de `_insert_manual_position` bajo Postgres (pgshim): leí la ruta SQLite y el `conn.execute`/`lastrowid`, pero no verifiqué que `cur.lastrowid` funcione igual bajo el shim de backend/main.py:~415.
- Si el `token TEXT NOT NULL UNIQUE` de `advisor_reports` puede colisionar en la práctica con 22 chars de `token_urlsafe(32)` — no hay try/except sobre el INSERT en el generador, así que una colisión saldría como 500. No lo pude cuantificar sin datos.
- Si existe algún test que cubra la divergencia ART/UTC entre `advisor_book` y `advisor_book_detail`: no busqué en backend/tests/ (fuera del alcance del tramo).
- Cuántos usuarios reales están hoy afectados por la falta de `_floor7` en `/book/detail`: requeriría consultar datos de usuarios, cosa que las reglas prohíben.