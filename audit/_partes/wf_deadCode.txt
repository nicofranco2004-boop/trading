- backend/seed.py — DEPRECATED con sys.exit(1) a nivel de módulo (backend/seed.py:7-10). Nadie lo puede importar ni correr. backend/pgshim.py lo llama 'código muerto' en su docstring, y sus INSERT OR REPLACE quedaron sin convertir a propósito.
- start.sh — el script entero es inútil salvo en el caso en que la base ya existe y tiene posiciones, porque su rama de bootstrap (start.sh:11-26) importa seed.py y muere. Con set -e (start.sh:2) el script termina ahí.
- backend/billing/mercadopago.py — declarado LEGACY en backend/.env.example:71-74 ('DEPRECATED: migramos a Rebill… el código nuevo (billing/rebill.py) las ignora. Eliminar este bloque después de 30 días sin problemas en prod'). El archivo sigue en el árbol y sigue leyendo MP_ACCESS_TOKEN, MP_ENV, MP_WEBHOOK_SECRET, MP_TEST_PAYER_EMAIL, MP_BACK_URL_BASE. Nota: MP_FRONTEND_BASE_URL NO es muerta — la usa Rebill (backend/billing/rebill.py:80) con el nombre histórico.
- backend/dberrors.py:34-39 — la rama `except ImportError` que asume 'sin psycopg (producción hoy): SQLite solo'. Con psycopg[binary]==3.2.13 en backend/requirements.txt:36 esa rama ya no se ejecuta en Railway, y su comentario (:14-16) afirma lo contrario de lo que dice requirements.txt.
- AUDIT_REPORT_2026-05-25.md y ONBOARDING_REPORT_2026-05-26.md — bitácoras de sesión de mayo. Ningún archivo del repo las cita por nombre (grep de 'AUDIT_REPORT_2026-05-25' en backend/, frontend/ y .gitignore da cero).
- El mecanismo VITE_GA_MEASUREMENT_ID descrito en frontend/src/utils/analytics.js:3,52 y frontend/src/main.jsx:12 — no existe: el ID está hardcodeado en frontend/src/utils/analytics.js:37. Ídem el pixel en frontend/src/utils/metaPixel.js:22 y frontend/index.html:204,214.
- backend/main.py:3789 — el 'empezar de cero' está desactivado tras RENDI_RESET_DATA_ENABLED != '1'; el worker y el endpoint de status quedan inalcanzables salvo que alguien prenda la variable.
- backend/seed.py — script entero muerto: `sys.exit(1)` a nivel de módulo (seed.py:10). Además sus `INSERT INTO config VALUES ('tc_mep','1415')` (seed.py:29-30) son inválidos: 2 valores para 3 columnas. El propio archivo lo documenta (seed.py:21-27).
- backend/main.py:1562 — `futures_positions.liquidation_price`: se escribe en main.py:12254 y 12276, y NO tiene ningún lector (grep en todo el repo: sólo DDL, modelo Pydantic, los 2 writes y schema_pg.sql:875). Ni siquiera hay input en `frontend/src/components/FuturosGroup.jsx`.
- backend/main.py:1528 — `plazos_fijos.renovacion_auto`: se guarda (main.py:9152, 9179) y se castea a bool en el GET (main.py:9114), pero ninguna lógica lo consulta. El único uso en frontend es el checkbox del propio formulario (PfFormModal.jsx:143, 201, 352). No existe renovación automática.
- backend/main.py:1618 — `watchlist.asset_type`: se escribe (main.py:33307) y se devuelve en el JSON (main.py:33274), pero ningún componente lo lee (grep sin resultados en components/home/Watchlist.jsx, fundamentals/useWatchlist.js, fundamentals/StarToggle.jsx).
- config key `display_currency` — leída en backend/ai/builders/metrics_pro_card.py:126-130 y escrita por NADIE. El frontend la guarda en localStorage (`rendi_display_currency`, frontend/src/contexts/CurrencyContext.jsx:24). La lectura siempre devuelve el default 'USD'.
- backend/main.py:1203 — `operations.cost_basis_consumed`: escrita en un solo camino (main.py:10484) y declarada '100% NULL en las filas reales' por tres lugares del código: main.py:37436, frontend/src/utils/assetPnl.js:18, frontend/src/utils/bookComposition.js:210. Todos los lectores la descartan.
- backend/main.py:1129 — `operations.pnl_pct`: casi-muerta. Se persiste en 3 caminos (main.py:10957, 11301, 13854) y el único lector es un diagnóstico admin de escala (main.py:11611, 11639-11654). Ningún componente de producción del frontend la usa.
- backend/trading.db — `users.gift_plus_email_sent_at`: columna presente en la base de dev que NO existe en el código actual (el código usa `gift_plan_email_sent_at`, main.py:795-799). Residuo de una versión anterior.
- backend/price_history.py (módulo completo) — sin ningún importador en producción: sólo lo importa backend/ledger_replay.py:213, que a su vez no lo importa nadie fuera de tests (las únicas menciones son comentarios en backend/twr.py:1232 y backend/importing/proyeccion.py:12)
- backend/ledger_replay.py (módulo completo) — sin caller en producción; sólo backend/tests/test_advisor_plan.py
- Tabla asset_price_history (backend/main.py:1788) — sin escritor ni lector conectado a ningún endpoint o cron
- Tabla price_backfill_log (backend/main.py:1802) — ídem
- advisor_clients.consent_ref (backend/main.py:854) — 3 escrituras (:34404, :34949, :34959), 0 lecturas en todo el repo
- advisor_clients.revoked_at (backend/main.py:856) — 3 escrituras, 0 SELECT
- advisor_op_batches.op_kind (backend/main.py:870) — constante 'buy' hardcodeada en el único INSERT (backend/main.py:36247), nunca leída
- advisor_op_batch_items.status = 'error' (backend/main.py:879) — valor documentado en el DDL que nunca se escribe (sólo 'ok' y 'undone')
- advisor_alert_events.kind = 'brief' (backend/main.py:1754) — valor documentado que nunca se escribe; el único INSERT usa 'client_move' (backend/advisor_alerts.py:309-313)
- twr_periods.snap_id_start / snap_id_end (backend/main.py:1832-1833) — escritos en backend/twr.py:826-830, ningún SELECT los lee pese al comentario 'para poder auditar de dónde salió'
- twr_periods.fx_basis (backend/main.py:1834) — constante 'mep_medio' hardcodeada (backend/twr.py:790) y nunca leída
- twr_periods.sealed_at (backend/main.py:1836) — sólo aparece en el DDL
- user_broker_credentials.scope (backend/main.py:738) — constante 'read' en los 3 INSERT, nunca leída ni comparada
- asset_price_history.source y .fetched_at (backend/main.py:1791-1792) — escritas, ninguna query las lee o filtra
- price_backfill_log.fetched_at (backend/main.py:1805) — escrita en cada upsert, nunca leída
- advisor_alerts.updated_at / advisor_profile.updated_at / advisor_brief_log.sent_at / alert_symbol_state.updated_at / broadcast_send_log.sent_at — todas escritas, ninguna leída
- backend/main.py:1258 — `news.image_url`: columna muerta, el único INSERT (backend/main.py:6678-6679) pasa NULL literal. 0 filas no nulas en la copia de dev.
- backend/main.py:1262 — `news.tickers`: ídem, NULL literal en el único INSERT. 0 filas no nulas.
- backend/main.py:1266 — `news.fetched_at`: se escribe y no se lee nunca (la frescura la decide _news_fetched_at en memoria, backend/main.py:6149).
- backend/main.py:1317 — `financial_events.fetched_at`: se escribe y no se lee nunca (frescura en _events_fetched_at, backend/main.py:5708).
- backend/main.py:1236 — `bond_indices_daily.source`: siempre 'argentinadatos', nadie lo filtra. Y `updated_at` (backend/main.py:1237) tampoco se lee: el stale se calcula contra _indices_fetched en memoria.
- backend/main.py:5600 — `fetcher_map = {"CER": _fetch_cer_series}`: de los 3 índices que el esquema declara (CER|UVA|A3500) solo CER tiene fetcher; UVA y A3500 devuelven serie vacía para siempre.
- backend/main.py:1480 — `fx_rates_daily.source`: se escribe con 4 valores distintos y ninguna query lo filtra.
- backend/main.py:1636 — `push_subscriptions.user_agent`: se guarda y no se lee en ningún lado.
- backend/main.py:1637 — `push_subscriptions.last_used_at`: solo se escribe al suscribir; _send_push_to_user (backend/main.py:34114-34163) no la toca. El nombre miente.
- backend/main.py:1963-1964 — índice `idx_yfinance_cache_fetched`: ninguna consulta ordena ni filtra por fetched_at (las dos que hay usan la PK (ticker,kind)).
- backend/main.py:2064-2065 — índice `idx_billing_events_user`: indexa una columna que nadie escribe (siempre NULL).
- backend/main.py:2058 — `billing_events.user_id`: columna muerta de escritura; ningún INSERT ni UPDATE la setea.
- backend/main.py:2028 — `subscriptions.amount_ars`: legacy de la era MercadoPago; se inserta siempre 0 pero los mails la siguen imprimiendo como plata.
- backend/ai/cache.py:170-176 — `cleanup_expired()`: función sin ningún caller en todo el repo.
- backend/pricing/fci.py:185 — `fci_catalog.activo`: nunca vale 0; no hay camino de baja para un fondo que desaparece de la fuente.
- backend/pricing/fci.py:200 — `fci_prices.fetched_at`: se escribe y no se lee (el que se muestra en UI es as_of_date).
- backend/main.py:2094-2103 y :2108-2117 — `email_verification_codes.created_at` y `password_reset_tokens.created_at`: se escriben por default y no los lee nadie.
- import_incidents (solo en la base de dev, backend/trading.db) — tabla completa sin ningún lector ni escritor en el código de b74f450f; ausente también de backend/schema_pg.sql.
- backend/main.py:2644 — `import_batches.override_info`: write-only por diseño declarado (se escribe en backend/main.py:30376 y no lo lee ningún SELECT). Está documentado como instrumento de medición, no como dead code accidental, pero hoy nadie lo consulta.
- backend/scripts/verificar_copia.py:415-418 — comentario obsoleto: afirma que schema_pg.sql tiene 58 tablas y le faltan fci_catalog/fci_prices; hoy tiene 63 CREATE TABLE e incluye las dos (backend/schema_pg.sql:788 y :819).
- backend/ai/quota.py:411-418 — `can_hub_query()`: sin call sites en backend/main.py (grep verificado)
- backend/ai/quota.py:439-449 — `record_hub_query()`: sin call sites en backend/main.py
- backend/ai/plan.py:33 y :52,72,89,109,125 — feature id `ai.hub`: no existe endpoint de Hub (grep de las 264 rutas: cero con 'hub') ni componente en frontend/src
- backend/ai/plan.py:38 y :58,78,94,114,130 — feature id `tax.helper`: el propio comentario dice 'próximamente (no construido)'; sin endpoint ni UI
- backend/ai/plan.py:35 — feature id `insights.distribucion_activo`: se sirve en /api/plan/features pero no lo lee nadie; en frontend/src solo aparece en utils/demo.js:2912, en un comentario de hooks/usePlanFeatures.js:10 y en textos de marketing (planCatalog.js, UpgradeModal.jsx, LockedSection.jsx)
- backend/ai/plan.py:48,68 — `insights_diagnostic_visible`: se calcula y se devuelve, pero no hay ni un `limit('insights_diagnostic_visible')` en todo frontend/src
- backend/ai/quota.py:453-465 — `record_chat()`: marcado LEGACY por el propio docstring ('pre B-9'), reemplazado por reserve_chat + record_chat_cost + refund_chat; se conserva solo para tests/scripts
- backend/main.py:3209-3211 — columna `users.approved` como gate de login: el comentario dice que el gate se removió y que la columna 'se mantiene (todos en 1) por compat de esquema'. Sigue siendo semánticamente viva para las cuentas shadow (approved=0), pero muerta como control de acceso al login.
- backend/main.py:2913 — EMAIL_CODE_RESEND_COOLDOWN_SECONDS = 60: definida y nunca usada en todo backend/ (el 60 está hardcodeado en el _check_rate_limit de resend-verification)
- backend/main.py:3330-3331 — try/except HTTPException: raise en verify_email: envuelve todo el cuerpo del handler para re-lanzar exactamente lo que ya se lanzaría
- backend/main.py:3759-3805 — POST /api/me/reset-data está detrás de RENDI_RESET_DATA_ENABLED != '1' y devuelve 503 fijo; con él quedan inalcanzables en prod _reset_data_worker (3671), _borrar_en_chunks (3649) y el GET /api/me/reset-data/status (3741)
- backend/main.py:2721 — get_admin_user: no lo usa ningún endpoint de este tramo (sí fuera de él)
- backend/main.py:3462 + 3465 — reset_password cierra la conexión dos veces (except + finally); mismo patrón redundante en change-password (3584/3591), update_config (4594/4597) y post_snapshot (5087/5090)
- backend/main.py:8089-8092 — rama `elif not sym.endswith('.BA'): ... pass` en get_price_history: es la negación exacta del `elif sym.endswith('.BA')` inmediatamente anterior (backend/main.py:8087) y su cuerpo es `pass`. Nunca hace nada.
- backend/main.py:5600-5606 — la infraestructura de fetch de índices sólo tiene mapeado CER; las ramas para 'UVA' y 'A3500' de la allowlist (backend/main.py:5646) llegan a `if not fetcher: return` y no ejecutan nada útil. El endpoint queda funcionalmente muerto para esos dos índices.
- backend/main.py:6723 — `_ensure_news_for_query` no tiene ningún caller en el rango 5500-8500 (los dos endpoints de noticias usan `_ensure_news_batch_parallel`). No verifiqué el resto del archivo, así que puede estar usado desde otro lado o desde los tests.
- backend/main.py:7359 — `_bond_price_per1` no lo llama ningún endpoint de este tramo; el docstring dice que lo usa 'la normalización de unidad de bonos', que vive fuera del rango.
- backend/main.py:8111 — `except Exception as ex:` en get_price_history captura `ex` y nunca lo usa (el cuerpo es un comentario y `pass`, backend/main.py:8112-8113): la variable es código muerto y el error se pierde sin log.
- backend/main.py:8647-8648 — `_ai_cache_invalidate(uid)` + `return {"ok": True}` al final de `delete_position`: inalcanzable, todas las ramas del `with` retornan o levantan (incluidos los dos `except` en 8642 y 8646).
- backend/main.py:8768 — `_cached_ba_price` (23 líneas): ningún caller en todo `backend/` (verificado con grep recursivo; la única aparición es su propio `def`).
- backend/main.py:8791 — `_splits_solo_excluidos_por_fecha` (9 líneas): ningún caller en todo `backend/`.
- backend/main.py:8765 — `_SPLIT_PRICE_EVIDENCE_ENABLED = False`: constante que no se lee en ningún lado del código de producción (solo la menciona un comentario en backend/tests/test_importer.py:6475). Es el flag de la feature apagada de la que cuelgan los dos helpers muertos de arriba.
- backend/main.py:9015 + 9030-9033 — `evidencia = "fecha"` es una constante: nunca se reasigna, y el campo `evidence` de la respuesta de `/split-check` documenta un valor `'precio'` que el código no puede producir.
- backend/main.py:10184-10188 — el segundo parseo de fecha en `cash_flow` (`datetime.strptime` con `except ValueError: pass`): la rama de error es inalcanzable porque el `field_validator` de `CashFlowIn` (backend/main.py:9403) ya validó el formato con `_DATE_RE`.
- backend/main.py:10664 — `_cash_asset_for_currency` está viva (la usa `bond_cashflow` en 10502) pero los dos call sites que más la necesitarían tienen el ternario copiado a mano: backend/main.py:10076 (reconcile-cash) y backend/main.py:10168 (cash_flow). Duplicación, no código muerto, pero del mismo origen.
- backend/main.py:10987 — `conn.commit()` redundante dentro de un `with conn:` (abierto en 10893) que ya commitea al salir; además solo está en una de las ramas.
- backend/main.py:9058 — `PlazoFijoIn.source_broker` es un campo del modelo que `PUT /api/plazos-fijos/{pid}` acepta y descarta por completo (el handler nunca lo mira).
- backend/main.py:13084-13086 — la rama `if mid.startswith('op-') or mid.startswith('pos-'): raise HTTPException(400, _TRADE_BLOCK_MSG)` dentro de `_delete_one_movement` es INALCANZABLE desde HTTP: `delete_movement` intercepta esos prefijos antes (backend/main.py:13148, :13156) y la función sólo tiene 2 callers (backend/main.py:13126 y :13167).
- backend/main.py:12473 — `op_type = (d.get('op_type') or '').upper()` en el loop de `operations` de `_build_movements`: variable local asignada y nunca leída (verificado con awk sobre 12468-12575).
- backend/main.py:12263 — `PUT /api/futures/{fid}`: sin ningún consumidor en frontend/src (FuturosGroup.jsx usa GET/POST/DELETE/close, ningún api.put). Superficie de API viva que hoy nadie llama.
- backend/main.py:12829 — `_TRADE_BLOCK_MSG` ('El borrado de compras/ventas llega pronto') está desactualizado: desde que existen `_delete_operation_cascade`/`_delete_position_cascade` sí se borran. Uno de sus dos usos es el código inalcanzable de :13086.
- backend/main.py:13294 — el comentario '# BOM para que Excel español detecte UTF-8' documenta un comportamiento que el código no implementa: `_csv_response` no emite BOM.
- backend/main.py:11038 — el docstring de `sync_unrealized` dice que 'Phase 8 moverá ese rollover al backend'; el rollover YA está en el backend (backend/main.py:11970) pero este endpoint sigue sin dispararlo, así que el TODO quedó a medias.
- backend/main.py:15293 — `_cagr_from_monthly_rows`: sin ningún call site en el repo (único referente: su mención en el docstring de `_historical_cagr_global` en backend/main.py:15298 y un comentario en backend/tests/test_cagr_snapshots.py:83).
- backend/main.py:13294-13299 — el comentario '# BOM para que Excel español detecte UTF-8' en `_csv_response` no corresponde a ningún código: el BOM nunca se escribe.
- backend/main.py:13876-13877, 14357-14360, 15119-15126 — los `_update_monthly_pnl_realized` previos al recalc son hoy redundantes (inofensivos: el recalc pisa con SUM(operations)); el comentario que los justifica describe una versión de `_recalc_pnl_realized_from_ops` anterior al seeding de meses de backend/main.py:9452-9467.
- backend/main.py:14311-14312 — el mensaje `_stale` duplicado inline, existiendo ya el helper `_stale_undo_msg()` en backend/main.py:14283.
- backend/main.py:2518 — comentario del esquema `-- 'imported' | 'manual'` desactualizado: el código escribe cuatro `kind` distintos.
- backend/main.py:16867 — string mostrado al admin con la referencia de línea 'main.py:10985' para una función que hoy está en backend/main.py:15623.
- backend/main.py:16927 — `import shutil, tempfile` dentro de `admin_fx_migrate_user`: `shutil` no se usa en toda la función (el clon se hace con `sqlite3.Connection.backup`).
- backend/main.py:17254 — `import shutil, tempfile` dentro de `admin_fx_migrate_batch`: mismo import muerto de `shutil`.
- backend/main.py:17077 — `except dberrors.EnsayoPorClonNoDisponible: raise` en `admin_fx_migrate_candidates`, un endpoint que NUNCA clona la base. Rama inalcanzable, copy-paste de los que sí clonan.
- backend/main.py:17827 — `_user_has_positions` se importa desde `reporting.builder` en `admin_diagnose_reportes_basis` y no se usa en ninguna parte del cuerpo.
- backend/main.py:17828 — `MEDICION` se importa desde `twr` en el mismo endpoint y tampoco se usa (sólo aparece en comentarios y en strings de prosa).
- backend/main.py:16752 + 16850 — `k_tipico` en `diagnose-scale` es código efectivamente muerto en su intención: `por_activo.setdefault(..., {"k_tipico": k})` fija el PRIMER k visto y nunca lo actualiza, pero se publica en el JSON como si fuera un valor típico del activo.
- backend/main.py:18045 — el `except sqlite3.Error` que protege el bloque `picos_implausibles` es código muerto bajo Postgres: `backend/pgshim.py` no mapea excepciones de psycopg a `sqlite3.Error`, así que el caso que el comentario dice cubrir ('deploy a medio migrar') no queda cubierto.
- backend/main.py:18532-18564 — todo `_db_internals()` de `/api/admin/disk-usage` es inalcanzable en Postgres: `PRAGMA page_size`/`page_count`/`freelist_count` y `dbstat` no están traducidos en pgshim, así que el bloque entero cae al `except Exception` y devuelve `{"error": ...}`.
- backend/main.py:18485 — GET /api/admin/disk-usage: sin ningún caller en frontend/src. Herramienta de diagnóstico de un incidente de disco lleno que quedó publicada.
- backend/main.py:18590 — POST /api/admin/delete-snapshot: sin caller en frontend/src.
- backend/main.py:18718 — GET /api/admin/ai/tool-usage: sin caller en frontend/src, pese a estar completo y documentado.
- backend/main.py:19551 — GET /api/admin/billing/inspect: sin caller en frontend/src (el panel sí llama a restore-tier y grant-comp, pero no a inspect).
- backend/main.py:19985 — GET /api/admin/fci/probe: artefacto de 'Fase 0' de la integración de FCI; sin caller en frontend/src. La decisión de go/no-go ya se tomó (la integración existe: backend/pricing/fci.py).
- backend/main.py:20098 — GET /api/admin/pf/probe: artefacto de 'Fase 0' de plazos fijos; sin caller en frontend/src, y reimplementa a mano el helper _probe de fci/probe.
- backend/main.py:20273 — POST /api/admin/wipe-broker-data: sin caller en frontend/src y duplicado funcional de POST /api/imports/wipe-broker (backend/main.py:31740), que además está mejor protegido contra locks.
- backend/main.py:18784 — línea del docstring de /api/admin/plan/conversion que documenta dos campos (distinct_users_blocked, distinct_users_clicked) que el endpoint no devuelve.
- backend/main.py:19426 — línea del docstring de /api/admin/email/broadcast que afirma que no hay idempotencia, contradiciendo el content_hash que sí existe en 19509-19532.
- backend/main.py:20391-20394 — bloque de comentario que documenta código YA ELIMINADO (/api/ai/insights legacy, AIInsightsIn, _AI_SYSTEM). Lápida útil hoy, ruido mañana.
- backend/main.py:20075 — parámetro `uid` de fci_catalog: se resuelve con get_effective_user (con su decode de JWT + SELECT users + posible query a advisor_clients) y nunca se usa en el cuerpo.
- backend/main.py:21034 — YF_CACHE_TTL_SECONDS existe solo como alias back-compat de YF_CACHE_TTL_DEFAULT_SECONDS 'para tests existentes'; ningun path de produccion del rango lo lee (todos usan YF_CACHE_TTL_BY_KIND.get(kind, YF_CACHE_TTL_DEFAULT_SECONDS) en backend/main.py:21125).
- backend/ai/cache.py:52 — CACHE_TTL_SECONDS, mismo patron: alias back-compat que defaultea a Pro; el codigo vivo usa _ttl_for_tier (backend/ai/cache.py:57).
- backend/main.py:21151-21154 — el comentario describe una sanitizacion ('solo cacheamos si no devuelve available:False por error transitorio') que el codigo no implementa. Es documentacion muerta que describe un guard inexistente, y ademas induce a error a quien lea.
- backend/main.py:26014 y 26026 — la rama de upgrade del 429 es inalcanzable para el caso del asesor dentro de un cliente Free/Plus: el tier ya fue reescrito a 'pro' en 25942, asi que upgrade_available siempre da False en ese camino.
- backend/main.py:21157-21162 — la rama else de 'fetcher devolvio non-dict' solo se alcanza si un fetcher retorna algo que no es dict; los 6 fetchers del rango (21340, 21441, 21529, 21798, 21873, 21943) retornan dict en todos sus paths, incluidos los de excepcion. Defensiva, no alcanzable hoy.
- backend/main.py:25858-25860 — las descriptions '2 a 4 fortalezas' / '1 a 3 riesgos' de FundamentalsAISummary no tienen efecto de validacion (no hay min_items/max_items); son solo texto para el LLM.
- backend/main.py:27476 — `GET /api/billing/status`: sin ningún caller en `frontend/src` (grep vacío). Los únicos consumidores son los tests (`backend/tests/test_billing.py:244`, `:259`). El docstring dice que lo usa /planes, pero `frontend/src/pages/Planes.jsx` no lo llama.
- backend/main.py:28412 — la segunda cláusula del `or` (`uid != _lens_auth and quota.get_tier(conn, _lens_auth) == "advisor"`) es inalcanzable en la práctica: `conn` se cerró en `:28342` y `get_tier` devuelve 'free' ante excepción (`backend/ai/quota.py:219-221`), así que nunca es True.
- backend/main.py:27501 — `POST /api/billing/webhook` (MercadoPago) es un camino legacy: la app cobra por Rebill (`backend/main.py:26522-26523`) y ningún flujo vivo genera preapprovals de MP. Sigue montado, con el fail-open descrito.
- backend/main.py:27420 — `POST /api/billing/sync` consulta MercadoPago con ids que hoy son de Rebill; funcionalmente muerto pero SÍ lo llama `frontend/src/pages/BillingReturn.jsx` (o sea que es un 502 en la pantalla de retorno del checkout, no código sin llamar).
- backend/main.py:28682 — `_reserved = True` es una asignación muerta: la variable nunca se lee fuera del `else` que la vuelve a asignar (`:28687`).
- backend/main.py:26534 — `SELECT email, tier, is_admin` en `billing_subscribe`: `tier` e `is_admin` se leen y se descartan; solo se usa `email` (`:26538`).
- backend/main.py:26750 — `track_event = "subscription_plan_changed"` se asigna solo para interpolarlo en un `log.info`; no hay tracking real detrás.
- backend/main.py:27516 — `from billing import mercadopago` dentro de `billing_webhook`: el nombre nunca se usa en la función (los helpers lo re-importan en `:27590`).
- backend/main.py:26716 — `note=f"User-initiated plan change from /api/billing/change-plan"`: f-string sin ninguna interpolación.
- backend/main.py:30125-30170 — la rama `else` del path de foto de tenencia es INALCANZABLE: la condición de backend/main.py:29891 es `is_ppi or is_balanz or is_ieb or is_cocos or is_bullmarket or is_iol or is_inviu` y la cascada de detección de formato (29694-29787) prende exactamente uno de esos siete flags o levanta HTTPException(400) en 29784-29787. Incluye el sub-`if` de 30162 y su `else` de gap-fill puro en 30169-30170 (el único camino sin override).
- backend/main.py:30131-30137 — dentro de esa misma rama muerta, el SELECT que arma `current` es trabajo descartado: `current` se pisa entero en 30139 con `_qty_a_fecha(pair)`. Sólo `invested_by_asset` sobrevive.
- backend/main.py:29437, 29453, 29459, 29467, 29499 — el parámetro `uid` de /api/imports/template, /parsers, /parsers/grouped, /inspect y /classify-tenencia nunca se usa en el cuerpo; el Depends está sólo para forzar el login.
- backend/main.py:31665 — `GET /api/imports/mappings` (`import_mappings_list`): ruta inalcanzable, siempre la captura `GET /api/imports/{batch_id}` registrada 28 líneas antes (31637). La función existe y es correcta, pero nunca se ejecuta.
- backend/main.py:32949 — `_ytd_delta`: no-op medido para todo `broker != "global"`. El propio comentario (32962-32979) dice que `_portfolio_snapshot_summary` fuerza `snap_value = None` con filtro por broker, así que la función muere en su primer `return None` y el cuerpo entero (el `ORDER BY month ASC LIMIT 1` y el arranque MEDIDO) nunca corre con un broker.
- backend/main.py:32635 — la clave `cash_value` del payload de `_portfolio_snapshot_summary`: el comentario de 32833-32852 verifica que su único uso en `frontend/src/` es un literal de fixture en `utils/demo.js:668`. Se calcula (una query más por request) y no lo lee nadie.
- backend/main.py:33236 / 33242 / 33250 — el parámetro `uid` de `home_indices`, `home_heatmap` y `home_movers`: se resuelve el JWT + SELECT users y después no se usa para nada; las tres respuestas salen de un caché de proceso global idéntico para todos los usuarios.
- backend/main.py:33232 — `from home.briefing import build_personal_cards` importado en este tramo pero usado recién en 34210, fuera del rango.
- backend/main.py:31719 — el `except Exception` de `import_recalc_pnl` (31733) atrapa también `HTTPException`; hoy el handler no levanta ninguna propia, así que la rama de traducción a 500 es inalcanzable para ese tipo — pero es el mismo patrón que en 31740 y 31890 sí necesitó un `except HTTPException: raise` explícito delante.
- backend/main.py:36962-36965 — import de `compute_broker_value_usd, build_price_symbols, read_last_prices, _broker_name_sets, _user_tc_cedear, position_price_key` dentro de `advisor_book`: los seis símbolos quedaron sin uso después de que la valuación se mudara a `_advisor_positions_valued` (verificado con `awk NR>=36948 && NR<=37245 | grep`).
- backend/main.py:36661-36662 — el `IndexError` del `except (ValueError, IndexError)` es inalcanzable: un slice de Python sobre un str corto no lo tira.
- backend/main.py:37452 — el `return []` de `_advisor_realized_raw` (rama `if not ids`) es inalcanzable desde los tres endpoints que la llaman, porque todos guardan `if not ids` antes; si alguna vez se alcanzara, `_advisor_realized_by_asset` reventaría con AttributeError sobre una lista.
- backend/main.py:36247 — `op_kind` está hardcodeado a `'buy'` y la columna existe en el schema (backend/main.py:870) con DEFAULT 'buy'; ningún código lee ni escribe otro valor, así que la columna y el badge `has_asset` que la acompañan (backend/main.py:36106) son andamiaje de una venta grupal que no existe.
- backend/main.py:37873-37874 — lambda inline de un solo uso `(lambda p: round(p,1) if p is not None else None)(_rate_pct(...))`: no es código muerto pero es un `round` con guard escrito de la forma más indirecta posible.