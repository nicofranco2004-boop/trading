backend/trading.db
backend/trading.db-shm
backend/trading.db-wal
backend/*.db
backend/*.db-shm
backend/*.db-wal
# Cualquier archivo SQLite local — el root también queda fuera del repo
*.db
*.db-shm
*.db-wal
# Snapshots / backups antes de operaciones destructivas (formato libre:
# trading.db.backup-..., trading.db.bak-..., dump.sql.bak, *.db.gz, etc.)
# IMPORTANTE: estos backups contienen datos reales de usuarios (emails + hashes
# bcrypt). NUNCA deben commitearse. Ver CORRECTNESS_AUDIT_2026-06-25.md (C4).
*.backup
*.backup-*
*.sql.bak
*.bak
*.bak-*
*.db.bak*
*.db.gz
backend/backups/
backend/.env
.env
.env.local
backend/__pycache__/
*.pyc
node_modules/
frontend/dist/
.DS_Store
